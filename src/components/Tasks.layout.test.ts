import { describe, it } from "vitest";

// 待辦面板的版面防線。舊版驗的是總覽表的 @container 窄視窗讓步（.tov-row／.tov-next），
// 票 19 把總覽表整段拿掉了；三個寬度等級的新版面與它的防線由 Task 10 一起落地。
describe("待辦面板的版面", () => {
  it.todo("Task 10 重寫");
});
