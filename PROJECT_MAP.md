# PROJECT_MAP.md — 專案模組地圖

> Claude Code 每次任務開始前必須讀取本文件，了解專案全貌與模組關係。
> 結構變更時必須同步更新本文件。

---

## 專案資訊

- **專案名稱：** Fledge — AI Workflow Studio（短名 `fledge`）
- **技術棧：** Tauri 2.x（Rust 殼）+ Python sidecar（FastAPI）+ React + TypeScript（Vite）
- **最後更新：** 2026-08-03

> 一句話定位：給 Claude Code 套圖形化 OS 殼，底層跑真實 `claude` CLI（繼承所有 skills/CLAUDE.md/MCP/帳號），上層 GUI 管理專案選擇、帳號分隔、多 sessions。

---

## 三層架構

```
Layer 1 · Tauri 殼（Rust）        → src-tauri/   啟動並管理 Python sidecar、IPC
Layer 2 · Python sidecar（FastAPI）→ sidecar/     專案掃描、PTY 橋接、設定讀寫
Layer 3 · Claude Code CLI          → 外部 subprocess（不修改，繼承一切）
React UI                           → src/         Zustand store + Sidebar/TabBar/多 Terminal 渲染
```

## 模組清單

### src-tauri/ — Tauri 殼（Rust）
| 檔案 | 職責 | 匯出 |
|------|------|------|
| `src/lib.rs` | entry：註冊 plugin、manage state、setup（先 orphan reap、生 per-launch token 存 state、再啟 sidecar；**spawn 失敗不 panic**——把原因寫進 `spawn_error` 後照常讓 webview 起來，否則啟動畫面的錯誤態與重試按鈕永遠到不了）、註冊 command（sidecar_port/restart_sidecar/sidecar_token/sidecar_spawn_error）、exit 收 sidecar | `run()` |
| `src/sidecar.rs` | sidecar 生命週期：spawn（dev/prod 統一 `std::process::Command`，dev=venv python、prod=`bundle.resources`/`resource_dir` 的 onedir exe，`.env(FLEDGE_TOKEN)`）、抓 `FLEDGE_PORT`、**統一持有當前 `std::process::Child`**、per-launch token（uuid v4、跨 restart 不變）、原子 pidfile、startup orphan reaper（pid-only 先禮後兵 `terminate_pid_gracefully`）、restart command（RestartGuard 互斥、kill 用 `try_wait` 收 zombie；重 spawn 前清 `spawn_error`、spawn 失敗立即回 `Err` 不進 30s 等待）、`sidecar_token` 查詢；**`spawn_sidecar` 回 `Result<(), String>` 而非 panic**——`.claude/tauri-rust.md` §4 允許 startup 用 `expect` fail-fast，但那適用於「無從補救」的前置條件，sidecar spawn 失敗（binary 遺失／無執行權限）是使用者可修復且值得被告知的狀態；失敗原因存進 `spawn_error` 供前端查（訊息帶完整 binary 路徑，會出現在啟動畫面的「詳細資訊」） | `SidecarState`, `generate_token()`, `spawn_sidecar()`, `sidecar_port()`, `sidecar_token()`, `sidecar_spawn_error()`, `restart_sidecar()`, `reap_orphan_sidecar()`, `kill_sidecar()` |
| `tauri.conf.json` | Tauri 設定，`bundle.resources` map 打包 onedir sidecar 資料夾（prod 走 `std::process::Command` + `resource_dir`） | — |
| `capabilities/default.json` | 權限：執行 sidecar、opener reveal、opener open-path（檔案樹雙擊開檔，scope `$HOME/**`）、dialog open（directory picker）、clipboard-manager read-text（終端機右鍵貼上走原生讀，繞 macOS Paste 膠囊） | — |

### sidecar/fledge_sidecar/ — Python sidecar
| 檔案 | 職責 | 匯出 |
|------|------|------|
| `__main__.py` | entry：拿空 port、啟 uvicorn、印 `FLEDGE_PORT=` | `main()` |
| `app.py` | FastAPI app 組裝、掛路由（含 `routes/backup`）+ `TokenAuthMiddleware`（驗 `X-Fledge-Token`、真 preflight 放行）+ CORS 收緊到 Tauri origin + 啟動認證狀態 log；lifespan startup 呼 `account_activity.mark_process_start`（活動 log 孤兒回收基準）、shutdown 關所有 PTY session | `create_app()` |
| `auth.py` | sidecar 認證純函式（fail-closed 預設 + `FLEDGE_TEST_UNAUTH=1` opt-out；call-time 讀 env）：`token_ok`（constant-time）、`require_ws_token`（WS 共用） | `auth_disabled()`, `configured_token()`, `token_ok()`, `require_ws_token()` |
| `paths.py` | 路徑正規化純函式：`expand_and_validate`（拒空/相對）、`probe_dir`（os.stat 四態 dir/missing/not_dir/denied，不用 is_dir 以分 missing/denied）、`resolve_best_effort`（跟隨 symlink、永不 raise）、`canonicalize`；containment helper：`is_within_root`/`is_within_any_root`（以 root+os.sep 比對避免 prefix 偽命中）；目錄同一性 helper：`dir_identity`/`same_dir`/`is_same_or_within`（比 `(st_dev, st_ino)`——APFS 預設不分大小寫，字串比對判不出同一個目錄） | `expand_and_validate()`, `probe_dir()`, `resolve_best_effort()`, `canonicalize()`, `DirStatus`, `is_within_root()`, `is_within_any_root()`, `dir_identity()`, `same_dir()`, `is_same_or_within()` |
| `app_config.py` | `~/.fledge/config.json` 讀寫（原子寫）+ root/manual/override/account 寫入 helpers（含 account_references + remove_account 級聯 reassign）+ `kms_root`（KMS 根目錄，raw 含 ~、runtime 才 expanduser、不過 _migrate_path）+ `set_kms_root`+ `backup_dir`（備份輸出目錄，同 kms_root 的純量取向；**刻意無預設值**——備份放哪是使用者必須有意識做的決定，合法性一律由 route 驗，放進載入路徑會讓事後失效的值擋死啟動）+ `set_backup_dir`；load 時自我遷移既有路徑成 canonical（resolve）+ 依 canonical 去重；`DEFAULT_CONFIG` 是**單一帳號**（key `default`、`config_dir` `~/.claude`、`label` 空——UI 缺 label 退回顯示 key，後端因此不輸出 user-facing 文案），多帳號由使用者自己在設定頁加；設定檔不存在時整份取用本表，**有 `accounts` 欄位的既有 `config.json` 不受影響**——唯一例外是設定檔存在卻缺 `accounts`（`data.get` 會讓它也拿到本表；那種檔案 `to_dict()` 寫不出來，只可能來自手動編輯，刻意不為它保留 legacy fallback）；另有嚴格版 `load_existing()`（單次讀取、檔案級與 accounts 欄位級**永不 fallback**——破壞性端點〔移機 install〕專用：不存在拋 FileNotFoundError、壞 JSON／缺 accounts 拋 ValueError；解析主體抽 `_from_data` 與 load 共用）；`extra` 欄位（移機帳號外資產的使用者確認落點 `{name: raw path}`，票 07——adopt-config 寫入、install 端點讀取，非 dict 容錯為空）；`create_if_absent(build)`（共用初始化原語，spec §4.3.1——onboard 與 adopt-config 各寫一份 first-run 判定必然漂移；已存在拋 FileExistsError、呼叫端持 `_config_lock`；跨 process race 歸 Plan 04） | `AppConfig`, `DEFAULT_CONFIG`, `default_config_path()`, `create_if_absent()` |
| `project_scanner.py` | 根目錄 depth=1 掃描（標所屬 root、套 project_overrides）+ CC 用過記錄合併；逐 root best-effort 容權限（`scan_all` 回 `(projects, permission_error)`）；`encode_cc_project_dir` 編碼對齊 Claude Code（非英數→`-`）；`scan_all` recent 跨帳號 union | `scan_all()`, `scan_root()`, `encode_cc_project_dir()` |
| `dir_tree.py` | 列一層目錄純函式：`list_dir_entries`（排除 dotfiles、資料夾在前+名稱 casefold 升冪、內容層 no-raise，I/O 例外由呼叫端 wrap） | `list_dir_entries()` |
| `pty_bridge.py` | PTY 橋接、Session 管理（threading.Lock）、env 切帳號、resize；`create_session` 接受選填 `session_id`（route 先生成以記活動 log）、`on_close` callback（關閉通知→記 close 事件）、`live_ids()`（當前存活集合，給歸屬判 liveness）、`env_remove`（呼叫端指定額外剔除的 env key，install session 用）；child env 剔除 `FLEDGE_TOKEN`/`FLEDGE_TEST_UNAUTH`/`FLEDGE_PORT`（不灌 secret 進 claude，也不灌 sidecar 內部 port 進子進程）；`close_session` 是「先 pop 再 close」，`close()` 失敗時以 `isalive()` 決定 handle 去留——進程已死（最常見的失敗原因）照舊丟棄，仍活著才放回 registry（否則子進程失聯、`close_all()` 也回收不到），存活狀態問不出來一律當已死；**`on_close` 移到 close 之後、且只在「沒被放回」時才呼叫**——被放回代表進程還在跑，提前收尾會寫下 close 事件（`load_sessions` 讓它蓋過 `live_session_ids`）而使該 session 後續用量歸屬不到帳號 | `PtyBridge`, `Session` |
| `routes/health.py` | `GET /api/health`（附 `claude_found`：shutil.which 判 claude 是否安裝） | `router` |
| `routes/projects.py` | `GET /api/projects`（回 `permission_error`）+ `POST .../scan-preview`（onboarding 試掃計數、不落檔；回 `status` 判別碼 ok/denied/missing/not_dir/invalid，讓 wizard 預先擋無效 draft）+ `POST /api/projects/tree`（lazy 檔案樹；containment 順序：expand→realpath→先 explicit deny kms_root subtree〔含 child metadata 過濾〕→再限 allowed roots〔roots∪manual〕；error contract ok/invalid 400/forbidden 403/denied|missing|not_dir 200） | `router` |
| `routes/sessions.py` | `POST/DELETE /api/sessions` + `POST .../resize` + WS `/ws/{id}`（accept 前 `require_ws_token` 驗 `?token=`；模式 A：WS 斷不關 session；PTY EOF→close 4001+清 session、tie-break 以 is_alive 為準）+ flow control gate（connection-scoped `asyncio.Event`：WS text frame `{"type":"pause"/"resume"}` 經 `_apply_flow_control` 閘住 PTY→WS 方向；binary=PTY bytes、text 永不入 PTY；pump 1s timeout 仍可察覺 paused 中 EOF）；`CreateSessionRequest.kind`（claude/terminal/install/login/**backup**/**restore**，`extra="forbid"` 擋未知欄位注入）→`_resolve_command`：terminal 跑 `$SHELL -l`、login 跑 `claude`（OAuth）或 `codex login`（`login_target`）；install 走獨立分支：`get_install_command(install_id)` allowlist 查表（未知→400）、空 env_overrides + `env_remove=["CLAUDE_CONFIG_DIR"]`（最小 env、不歸屬帳號）；**kind=backup 走 `_backup_blocked()` 七道閘**（not_set→invalid→containment 三態→unusable→script_missing→python3_missing，順序即優先序且與備份卡顯示阻斷原因的順序一致；前端本來就 disable 按鈕，這層是防繞過——route 才是安全邊界不是 UI）後同樣走 `_unattributed_session`，argv 由 `build_argv` 從 config 組出、前端只送 `kind`+`backup_mode`；**kind=restore 走 `_restore_gate()`**（來源給了沒有→備份目錄〔**只有名字模式需要**〕→還原腳本→python3→備份包→展開位置，順序即優先序且與還原卡一致；請求本身壞掉優先於環境問題）；**閘放行時回的是它驗過的 `(bundle_abs, dest_abs)`，route 直接拿去組 argv 不重新解析**——閘裡算一組、組 argv 時又算一組的話，實際跑的可能是沒被 `check_dest` 驗過的另一個位置（`resolve_dest` 的撞名迴圈會探測檔案系統），重解析本身也可能拋 `ValueError` 變成裸 500〔隨身碟在這中間被拔掉〕（Codex 票 11 審查）；來源**二擇一**（票 11）：`restore_bundle` 是名字（過 `list_bundles` allowlist 才變成路徑）、`restore_bundle_path` 是使用者選的絕對路徑（**路徑模式跳過備份目錄那道閘**——新機器上 `backup_dir` 還是空字串是正常狀態，拿它擋只會擋死移機）；二擇一在 route 多判一次**不是為了規則而是為了判別碼**（本端點的缺席碼是既有的 `restore_bundle_required`，模組層是欄位中立的 `bundle_required`），實際解析仍只走 `restore.resolve_source_and_dest` 那一份；同樣走 `_unattributed_session`；其餘 kind 建立時驗 `req.account` 合法（未知→400）、**claude** login 注入帳號 env 但 **cwd 走 `Path.home()`**（登入不屬於任何專案、也不進活動歸屬，與 install 一致——精靈登入頁沒有專案路徑可傳；已知副作用：claude 會把 cwd 底下的 `.claude/` 當**專案級**設定讀入，而那恰好是 work 帳號的 `config_dir`——帳號 env 注入不受影響，只是 TUI 啟動時多讀一份設定，登入為一次性動作故不處理）；**`login_target=codex` 比照 install 完全不綁帳號**（codex 登入是全域的，B-1 收尾票已確認）——兩者共用 `_unattributed_session()`：跑在 home、不注入帳號 env 且主動剔除 `CLAUDE_CONFIG_DIR`、不進活動歸屬、claude session **spawn 前** `account_activity.record_open`（spawn 失敗補 close）、`on_close` wiring 記 close（僅有 record_open 的 claude session，`_opened_session_ids` gating）、暴露 `live_session_ids()` 給歸屬判 liveness | `router`, `live_session_ids()` |
| `routes/config.py` | `GET /api/config`（附 is_first_run）+ 細粒度寫入（roots/manual/overrides/accounts；add 類 canonicalize+驗存在、移除/改帳號類只 canonicalize）+ `POST .../onboard`（first-run 判定走 `app_config.create_if_absent`，票 07——與 adopt-config 共用原語與 `_config_lock`，語意不變）+ `POST .../check-dir`（回 `status` 判別碼）+ `PUT .../subscriptions`（訂閱費清單，name 非空/cost 有限非負）+ `PUT .../kms-root`（KmsRootBody Pydantic、`_config_lock`、回 `{ok,kms_root}`、不驗目錄存在）；account CRUD（級聯 reassign、key grammar、至少留1），鎖 + 驗證 | `router` |
| `routes/usage.py` | `GET /usage/dashboard?days=`（數據儀表板成本面板）：single-flight 掃描（25s stale gate、error 立即 retry、days 變更視為 stale）＋`asyncio.to_thread` 不卡 event loop＋last-good 語義（掃描中回舊資料標 scanning、冷掃 202+Retry-After、冷掃失敗 error-only 200）；模組級 `_state`（snapshot/cache/scanning）。另 `GET /usage/codex`：Codex 實時額度（呼 `api/codex_usage`）＋ `CODEX_USAGE_MIN_INTERVAL_SEC`(60s) 合併 floor（`_codex_state`），失敗回 typed unavailable 不 fallback 舊快照 | `router`, `reset_state_for_tests()` |
| `api/codex_usage.py` | Codex 實時額度抓取（api/ 層唯一對外出口）：讀 `~/.codex/auth.json` 的 `access_token`+`account_id`→GET `chatgpt.com/backend-api/codex/usage`（Bearer+`chatgpt-account-id`，8s timeout）→正規化 `primary/secondary_window`→`{used_percent,window_minutes,resets_at}`；**token 只進 header 絕不 log/回傳**，任何例外 redaction 成 typed 失敗（no_auth/unauthorized/network/bad_response），不外洩 raw exception | `fetch_codex_usage()`, `normalize_usage()`, `USAGE_URL` |
| `usage/pricing.py` | 計價公式與模型名正規化（別名/日期後綴/`<synthetic>`→None；**不做 prefix walk**，表外未知款一律 missing）＋兩源計價（USD per MTok ÷1e6、cache 三層讀表內真價、cached⊆input）；`PRICING_VERSION` 引用表的 `TABLE_VERSION` 供 L2 重算。`PINNED`（key→(價, 理由)）／`EXCLUDED`（key→理由）是刻意偏離上游的例外清單，只被 `admin/sync_pricing.py` 讀、不參與 runtime 查表 | `PRICING_VERSION`, `PINNED`, `EXCLUDED`, `normalize_*_model()`, `claude_cost()`, `codex_cost()` |
| `usage/pricing_table.py` | 定價表本體（**自動生成，勿手改**）：`admin/sync_pricing.py` 從 LiteLLM 整檔重生。Claude 五欄（input／output／cache 5m／cache 1h／cache read）、Codex 三欄（input／cached／output）。表 key 已套用 pricing.py 的正規化，故等同 runtime 查表用的 key；`TABLE_VERSION` = 生成日期 + 表內容 sha256[:8]，內容一變就變 | `TABLE_VERSION`, `CLAUDE_PRICING`, `CODEX_PRICING` |
| `usage/parser.py` | 兩格式逐行解析→瘦條目 `UsageEntry`（內容層不拋例外：壞行/壞值跳過計數；I/O 例外由呼叫端 wrap）：claude（快篩純最佳化、cache precedence nested 優先、dedup_key、sidechain）＋codex rollout（last/累計差分 clamp、turn_context model、session_meta cwd、rate_limits 末筆） | `UsageEntry`, `CodexFileResult`, `parse_claude_file()`, `parse_codex_file()` |
| `usage/scanner.py` | 兩源檔案發現（回傳一律 realpath）：claude＝各帳號 config_dir/projects 遞迴＋realpath 去重，另回 realpath→account_key 對應表（tie-break 按 account_key 排序取第一、不依 dict 序）；codex＝sessions/archived 的 per-session canonical selection（active>mtime_ns>size>路徑全序；首行窺視 session_id、_PEEK_LIMIT 1MiB） | `claude_files()`, `claude_account_map()`, `_scan_claude()`, `codex_files()` |
| `usage/cache.py` | L1 記憶體＋L2 磁碟瘦條目快取：(size, mtime_ns) 失效、bounded ThreadPool 平行 parse、`threading.Lock` 序列化、L2 損壞寬 catch 重建、pricing_version 變更只重算 cost 不重 parse、generation 防舊蓋新（同 schema 代才比較）、變動才落盤、atomic write；`RefreshResult` 另暴露 `claude_by_file`（realpath→entries shallow snapshot、只含 claude，防 route/測試 mutate 污染 L1） | `UsageCache`, `FileCacheEntry`, `RefreshResult`, `SCHEMA_VERSION` |
| `usage/aggregator.py` | dashboard payload 組裝：claude dedup 替換規則（parent>sidechain>token 多者）、synthetic 濾除、KPI（本地時區視窗/Net ROI/分源 cache 命中率）、daily/models/projects（memoized realpath）/hourly（7×24 週一起）聚合（900s bucket memo 消 datetime 成本）；payload 無 blocks（Codex 額度改走 `/usage/codex` 實時、Claude 5hr 卡片移除） | `build_dashboard()` |
| `usage/account_activity.py` | 帳號活動 log（補 jsonl 缺的帳號身分）：sidecar 自有 append-only JSONL（0600、module lock、**fail-open** 絕不阻斷 session 建立）；`record_open`/`record_close`（open/close 事件）、`mark_process_start`、`load_sessions`（pair open/close、liveness 以 bridge live 集合為權威 + read-time 孤兒回收：前一進程關於 process_start、本進程已不在 bridge 關於 now）、`attribute`（cwd containment + 最深匹配 + open_ts tie-break） | `record_open()`, `record_close()`, `mark_process_start()`, `load_sessions()`, `attribute()`, `SessionSpan` |
| `routes/memory.py` | 記憶層路由（唯讀聚合 + 唯一寫 memory-links.json）：`GET /memory/overview?q=`/`/related`/`/item`（單筆全文，realpath containment + allowlist 擋 root 外/`..`/逃逸 symlink）+ links CRUD（`POST`/`DELETE /links`、`POST /links/confirm`/`/dismiss`）；v1 scan-on-demand（`_scan` 各帳號 account-local known projects、KMS topic 以 folder 識別）；links.json 路徑可 `FLEDGE_MEMORY_LINKS` 覆蓋 | `router`, `reset_state_for_tests()` |
| `memory/parser.py` | markdown + frontmatter（YAML-lite）→ `ParsedDoc`；wikilink `[[..]]`（取 `\|` 前的 target、去重保序）；no-raise（OSError/解析錯→None，與 usage parser 同契約） | `ParsedDoc`, `parse_doc()` |
| `memory/scanner.py` | 找檔（對來源永遠唯讀）：`native_memory_files` 三態歸屬（encoded 反查：1 命中 matched／≥2 ambiguous／0 orphan，跳 MEMORY.md）+ `kms_files` 只掃 `library/`+`topics/`、`is_kms_file_allowed` realpath containment（擋 hidden/`_*`/CLAUDE.md/逃逸 symlink，與 route 共用） | `NativeRef`, `KmsRef`, `native_memory_files()`, `kms_files()`, `is_kms_file_allowed()` |
| `memory/links.py` | Fledge-owned `memory-links.json`（唯讀源外唯一寫入）：`LinksStore` 無向 (min,max) 去重 + dismissed 綁 pair + module-level lock 序列化並寫（read-modify-write 重讀）+ atomic write（tmp→rename）0600；`suggest_links` topic 名/tags 正規化⊇專案名（長度<4 只手動） | `LinksStore`, `suggest_links()`, `_norm()` |
| `memory/aggregator.py` | 組面板 payload：scope 分層（native user/feedback→global、project/reference→project、其餘→unknown；kms→kb）、專案中心分組（已連 topic 掛 project、unattributed/unknown 皆可見不靜默丟）、in-memory 搜尋（含 body、回 snippet）；**overview 只回摘要不含 body** | `MemoryItem`, `to_item()`, `build_overview()`, `build_related()` |
| `routes/setup.py` | `GET /api/setup/status`（開發環境偵測：回 `{tools:[ToolStatus…]}`，每筆含 binary／install_command／manual_command 供 UI 決定列什麼；安裝／登入不在此——走 `POST /api/sessions` kind=install/login）＋共通設置兩端點 `POST /api/setup/common-config/plan`（唯讀預覽）／`/apply`（實際套用）：Pydantic body `extra="forbid"`、apply 由 server 以相同輸入**重算 plan**（ADR-0002，不吃 client plan）、`setup_lock` 序列化並發 apply；錯誤分流 `_PlanError`——`config_unreadable` 500（`json.JSONDecodeError` 是 `ValueError` 子類，不可當判別碼外洩）／模組判別碼 400／`probe_failed` 500（探測期 `OSError`）；另有 `POST .../common-config/repair`（票 08 的斷鏈修復，還原卡的入口）——**與 apply 共用同一把 `setup_lock`**（兩者改寫的是同一批 symlink，各自持鎖等於併發互踩）、同樣 server 重算 plan、body 不收 `overwrite`（repair 從不做破壞既有內容的動作）、`repair` 的 `ValueError` 判別碼轉 400；apply 另設 readiness 閘 `config_not_initialized` 400（設定檔不存在時 `AppConfig.load()` 會 fallback 到 DEFAULT_CONFIG 的真實 home 目錄，預覽不設此閘）；範本三端點 `GET /api/setup/templates`（allowlist 全列、`available` 由 manifest 讀不讀得出來判定，未內建也照列）／`POST .../templates/plan`（唯讀預覽）／`POST .../templates/deploy`（server 重算 plan、走同一把 `setup_lock`），錯誤同合約（模組判別碼 400／`probe_failed` 500） | `router` |
| `routes/backup.py` | `GET /api/backup/status`（唯讀）：回 `configured`／`backup_dir`（raw）／`dir_status`／`containment`／`script_available`／`restore_script_available`（**還原腳本是另一個檔案**，打包漏收它時備份可用、還原不可用）／`python3_available`／`bundles`／`last_backup_ts`／`days_since`／`last_attempt_failed`（**環境旗標與 backup_dir 無關，未設定位置時也照回**——否則使用者選完位置才第一次得知這台機器跑不了備份）。**目錄問題不是錯誤**——一律 200 用旗標表達，把它們塞進 HTTP 錯誤碼就只剩「壞了」一個資訊，卡片無法分辨「不見了」與「沒權限」；正規化與探測共用 `expand_and_validate`（相對路徑回 `containment="invalid"`——`dir_status` 回答「這個目錄現在怎麼樣」，而相對路徑連目錄都稱不上，不拿 sidecar 的 cwd 去解讀出一個假答案）；**目錄不可用就不端清單**，掃目錄的整個重點是狀態要誠實 | `router` |
| `backup/restore.py` | 還原的決策層：`resolve_backup_dir`（備份包來源，**刻意不驗 containment**——那是輸出位置的規則，備份目錄位置不理想並不會讓裡面的備份包不能讀，擋下還原只是在使用者最需要它的時候製造死路）／`bundle_path`（**allowlist**：只接受 `list_bundles` 真的列出來的名字，路徑穿越與「存在但不是備份包形狀」都擋在這裡）／`resolve_dest`（未給就用 `default_dest`＝`~/.claude-restore-<備份包時間戳>`，**撞名往後加序號**〔比照 `common_config._backup`〕——跑過一次之後回到卡片，預設位置正是上次的展開結果，app 不該建議一個自己隨後會拒絕的位置〔真機驗收抓到〕；**使用者自己指定的位置不套這個**，他選什麼就是什麼、非空由 `check_dest` 照樣擋；plan 與 spawn 前的閘共用同一支，兩邊各自解讀必然漂移）／`check_dest`（與備份輸出目錄共用 `check_backup_dir`，另加空不空與型別探測；**containment 排在空不空之前**——位置結構上選錯時清空它也不會變成合理的位置）／`build_restore_argv`（同 `build_argv`：不經 `-lc`、顯式帶 `/bin/bash`）／**路徑模式**（票 11）：`bundle_from_path`（使用者用系統檔案選擇器挑的絕對路徑，**這條沒有 allowlist**——移機時包不可能在備份輸出目錄裡〔那目錄是拿來**寫**備份的〕；放行的能力基準線是 `kind=terminal`〔已能開登入 shell 執行任意命令〕**不是**「使用者選的所以可信」，該端點若被收窄本評估失效；仍當不可信路徑驗：絕對路徑→開得起來→是一般檔，**先 open 再 fstat** 且帶 `O_NONBLOCK`〔沒有它，FIFO 的唯讀 open 會阻塞而**根本執行不到型別判定**，實測〕，socket 的 open 直接 ENOTSUP 故另走 `stat` 回退分類；**symlink 不拒**〔前端大可直接送目標，拒絕只擋掉合理用法〕；**殘餘窗口**：驗完到 tar 以 pathname 重開之間仍是 check-then-act，只縮小不關閉）／`dest_for_bundle_path`（檔名不符備份包命名規則且未指定位置→`dest_required`，**不猜**——比照 `project_paths` 的建議值規則，basename 會變成展開目錄名的一部分，猜了就得 sanitize）／`resolve_source_and_dest`（二擇一分派，**plan 與 spawn 前的閘共用同一支**；兩者都給→`bundle_source_ambiguous` 不讓後蓋前，都不給→`bundle_required`）。**本模組沒有任何寫入現役目錄的能力**（票 09 驗收 #6 是結構性的，不是預設值） | `resolve_backup_dir()`, `resolve_dest()`, `bundle_path()`, `bundle_from_path()`, `dest_for_bundle_path()`, `resolve_source_and_dest()`, `default_dest()`, `check_dest()`, `build_restore_argv()`, `DestVerdict` |
| `backup/install.py` | **移機：唯一有能力寫現役目錄的還原路徑**（`backup/restore.py` 維持零寫入，術語分界見 CONTEXT.md）。`plan`（純唯讀：掃展開目錄與各落點，回會裝／會跳過／被祖先衝突擋住〔`blocked`：目的地中間元件是一般檔或 symlink 時該子樹葉檔到不了，算進 will_install 就是預覽說謊；逐層 lstat 與 install 的 `O_NOFOLLOW` 同語意、探測失敗 fail-closed〕／不處理；落點一律模組內 resolve＋ADR-0001 底線防呆，不採信 caller；extra〔帳號外資產，票 05〕落點須使用者逐項確認——未確認整項 excluded、name 與 account key 同規則重驗、落點驗證與帳號完全一致；extra 的正式 API 接線已完成（票 07）：adopt-config 逐項確認寫進 config、install 端點以 `extra=config.extra` 傳入，確認值非字串視同未確認（config 可手編，不裸 500）；`validate_landing_spots`（adopt-config 授權時刻驗證：key 文法驗 `extra:` 冒號後裸名、落點重驗、含帳號×extra 交叉的互為祖先拒 `overlapping_config_dirs`）；重疊檢查抽 `_ensure_no_overlap` 且 **`plan` 以 install 時的解析結果重跑一道**（帳號＋extra 一起；config 可手編、symlink 解析會變，授權時刻的檢查不能是唯一一道——Codex 票 07 R1 F2）；install 再加**寫入前 fd 版 pairwise 重驗**（Codex 票 07 R2：字串比對擋不住「plan 後才收斂」的落點——APFS 大小寫別名、中間目錄換 symlink；`_prepare_spot` 先開好全部落點 dst fd、`_identity_chain` fd-relative 逐層 `..` 算祖先鏈、`_drop_overlapping_spots` pairwise 比對同一目錄／互為祖先，涉入落點全 failed `overlapping_config_dirs`、零內容寫入；**縮小非關閉**——重驗返回後寫入中的 rename reparenting 屬票 09 第 3 項殘餘〔Codex 票 07 R3，裁示併票〕）；manifest 解讀權留在模組內——備份包內容是不可信輸入，只能描述來源不能授權目的地〔spec §4.2.2〕）／`install`（第一階段：目錄與一般檔；兩側 fd-relative——每層子目錄從父 fd 以 `O_DIRECTORY|O_NOFOLLOW` 開出、一般檔開檔後 `fstat` 判型才讀；身分驗證**先 open 再 fstat**〔顛倒仍有窗口、`O_NOFOLLOW` 不證明開到原 inode〕，來源身分不符回 `source_root_moved` **整批停手**、target 側同款重驗不符回 `target_moved` **該帳號停手**；**票 09 三項硬化**：①`target_anchors` 祖先釘鎖——plan 記（最深既存祖先＋身分＋剩餘元件），install 開祖先 fd 驗身分後逐層 mkdir(dir_fd)＋O_NOFOLLOW 往下建（EEXIST 容忍），取代 Path.mkdir(parents)＋絕對路徑重開，祖先重導結構性關閉（票 03 R5 殘餘收攏）；②journal 記 node `(dev, ino, kind)`（目錄對 child fd、檔案由 `write_bytes_atomic` 回傳暫存 fd 身分），第二階段 `_node_identity_matches` 從落點 root fd 逐層 O_NOFOLLOW 開到 node fstat 比對後才授權，**驗證失敗判 failed `node_identity_mismatch` 不判 excluded**（excluded 放行 journal 清除→重跑 EEXIST 不再記→連結永久補不回）；公開 `installed_nodes` 維持寬鬆集合（還原卡偵測），授權走嚴格版 `_parse_journal_records`（缺身分＝不授權）；③`dir_owners` reparenting 防護——各落點 root＋本輪開出子樹＋前輪 journal dir 記錄起底，寫入中開到別家目錄即 `_ForeignSpotError` 雙方停寫 `overlapping_config_dirs`（跨輪起底走嚴格版 `_parse_journal_records_strict`——journal 非空但讀不出／解不出／任一行不完整即全體 fail-closed `provenance_unavailable` 零寫入；寬鬆容錯只留公開 `installed_nodes`）；symlink 發布改**暫名原子模型**（比照 `write_bytes_atomic`：暫名 symlink→驗 node→`os.link(follow_symlinks=False)` 同一 parent fd 原子發布→清暫名；最終名只在驗證通過後出現、從不回滾，EEXIST=skipped；暫名 pathname TOCTOU 記為與 write_bytes_atomic 共享的既定殘餘——Codex 票 09 五輪 R5 approve）；帳號級 `except OSError` 隔離（落點被占用／權限被收走只讓該帳號 failed，不株連也不讓例外穿出變裸 500）；extra 與帳號走同一支落點安裝〔`extra:<name>` 命名空間，identity 重驗與隔離同款〕，extra 來源**不存在≠開失敗**——前者正常靜默、後者記 failed 保留 journal〔extra/ 與項目兩態，票 05〕，且**帳號與 extra 一體**另綁 **plan 時來源身分**〔`spot_source_identities`（key＝落點命名空間）＋共用 `_open_verified_source`；Codex 票 05 R1 F1、帳號側比照＝票 05 收尾裁示（封票 03 殘餘窗口）：換成另一個真目錄（O_NOFOLLOW 攔不到）、plan 後才出現、plan 看過卻消失皆 `source_moved` failed，開失敗（symlink／權限）記 failed；None＝plan 時不存在，ENOENT 才容許靜默；內容層變動不凍結——plan 是預覽不是內容授權〕；寫入走 `safe_fs.write_bytes_atomic`，EEXIST→skipped 冪等；**續作時每層進場先 `safe_fs.find_stale_temps(dst_fd)` 指認暫存殘骸**（票 08：前一輪硬中斷留下的，**只收集不刪除**——收齊後在 install 收尾以 WARNING log 出「發現 N 個（未自動刪除）」＋前 20 個位置，刪除授權留給使用者〔票 08 R3〕；掃的範圍就是本輪會寫入的目錄、不額外走訪使用者的其他位置，因此本輪不會走到的位置指認不到；**`stale_temps` 非 None〔＝有前輪未收尾〕才掃**——journal 完整成功會被清除，故「還有 journal 在」正是有一輪沒收尾的訊號、也是唯一可能有殘骸的情況，首次安裝掃了必然一無所獲而該掃描是**按目的地既有目錄項計費**〔Codex 票 08 R1 F3〕；判準走 `_has_unfinished_journal()`＝**有沒有任何一輪沒收尾**，不是「本次 transaction 的 journal 在不在」〔Codex 票 08 R2：`transaction_id` 綁 source_identity／落點／renames，中斷後重展 bundle 或改 mapping 再跑會讓新 journal 尚不存在而永久漏清，且新 transaction 成功後又清掉自己的 journal〕——粒度**刻意比 provenance 寬**，這只決定要不要多花一次掃描、誤判方向是多掃，provenance 授權仍嚴格綁本次 transaction，兩者不共用判準；這不是安全判斷，故用 pathname 探測）；特殊檔（FIFO 等）**連 open 都不碰**判 excluded（一開就阻塞）且 plan 只數 lstat 一般檔——兩邊掃描語意一致預覽數字才不虛報；`.claude.json` 一律 excluded；每目錄離開前含 **account root 層** `fsync(dst_fd)` 承擔斷電下目錄項持久性〔spec §4.2.5〕；**兩階段發布**〔票 04〕——第一階段目錄與一般檔、symlink 收集起來延到第二階段〔`_symlink_at` 全程 fd-relative：每帳號第一階段驗過身分的 `dst_fd` 延到第二階段、逐層 `O_NOFOLLOW` 開父目錄再 `os.symlink(dir_fd=父)`，不重解析 pathname——階段間換 target root／中間目錄不跟隨，票 04 R1 F2〕，判準是 provenance「目標對應本次 transaction 確實發布的 node」而非 root containment〔後者對目錄等於一次暴露整棵既有內容，spec §4.2.3〕；字面目標相對路徑／含 `..`／非正規化一律源頭 fail closed，home→home 前綴改寫，授權集合含本次新建的目錄 node〔已存在的不記，那是使用者的〕、落點涵蓋 extra〔`_all_landing_spots`：`~/.agents` 的資產能作 symlink 授權目標，票 05〕）／**provenance journal**〔票 04，`~/.fledge/restore-journal-<tid>.jsonl`〕：`transaction_id`（綁 source_root＋`source_identity` 實體 inode＋落點 mapping——三者任一變即新 transaction，不讀舊 journal，票 04 R1 F1）／`journal_path`／`installed_nodes`（壞行跳過；**不存在回空、讀不出拋 OSError**——第二階段據此判降級、pending 判 failed 保留 journal，票 04 R1 F3）；journal fd **走 fd-relative 開啟且 O_RDWR**〔`_open_journal_fd`：從 home fd 逐層 `O_NOFOLLOW` 開 .fledge 再開 basename、fstat 一般檔——`~/.fledge` 或 journal 被換 symlink 時不跟隨、不 append 到別的檔案，票 04 R2 F1；**第二階段 provenance 讀取從這個 pin 住的 fd `pread` 全檔〔含前輪 append 故中斷續作天然涵蓋〕、不重解析 pathname**——開啟後被換 symlink 也讀不到偽 journal，票 04 R3；解析共用 `_parse_journal_nodes`〕；發布成功才記〔非 intent-first〕、**完整成功（無 failed）才清除**〔ADR-0006 的「未收尾」訊號，有 failed 保留供中斷續作認前一輪〕／`read_manifest`（讀不到、非物件、`accounts` 非 mapping、`extra` 非 {str: str}→`source_not_a_bundle`；實體 `accounts/` 缺失或非真目錄同判——預覽與執行同一判準，不讓 TypeError／OSError 穿出成裸 500）；account key 在模組信任邊界以 `^[A-Za-z0-9_-]+$` 重驗（絕對 key 讓 Path 丟棄 root、`..` 走出 staging、os.open 對絕對路徑忽略 dir_fd；manifest 不可信、config 可被手動編輯，違者 `invalid_account_key`）／**專案路徑改寫**〔票 06；票 01 實測：/resume 定位只靠 `projects/<encoded>` 目錄名〕：`plan(..., mapping=[(舊絕對路徑, 新絕對路徑)])` 前置驗證一次驗完（old／new 皆須絕對路徑 `mapping_not_absolute`／old 必須是包內存在的專案 `mapping_unknown_project`／**old 以 cwd 驗身**——encoded 名是有損投影，每個命中目錄重讀 cwd 逐字比對、不一致含讀不出皆 `mapping_ambiguous`〔Codex R1 F2：防誤中無關專案與跨帳號同名誤動〕／編碼有損的碰撞——新名互撞・撞未改寫既有名・同 old 重複皆 `mapping_collision`），`project_renames`（舊 encoded→新 encoded，新名一律 `encode_cc_project_dir` 產生）與 `unmapped_projects`（照搬原位、前端據此提示 /resume 找不到）進 `InstallPlan`、改名表綁進 `transaction_id`；掃描與寫入以 `_dest_rel` 統一用**目的地**位置判 skip／blocked／journal（帶 mapping 重跑才冪等）；`_install_tree` 只在帳號根層進 `projects/` 那一步把改名表傳成下一層 `rename_children`（extra 不適用）；歷史檔逐位元組不變、staging 全程唯讀；symlink 指進被改名專案的目標跟著改名（`_authorized_link_target` 對帳號落點 rel 套 `_dest_rel` 再查 journal、回寫目的地位置——否則 journal 記新名查詢用舊名、連結誤判 unauthorized 丟失，Codex R1 F3）；`project_paths()` 唯讀列專案（`_peek_cwd` 逐行找首個 cwd——首行可能是無 cwd 的 summary；舊 home 底下給建議值、其餘留空不猜；**讀取面同套不變式**〔Codex R1 F1〕：fd-relative＋`O_NOFOLLOW|O_NONBLOCK`＋fstat 判型——jsonl 可能是指向 staging 外的 symlink 或 FIFO，且檔數／位元組有上限——檔數上限用 `heapq.nsmallest` 在列舉階段就生效〔O(上限) 記憶體、免全量排序，Codex R2；列舉線性時間＝plan/install 全樹 walk 既有基線〕，語意是「只開排序後前 N 個」**不是**「超過 N 個整批放棄」（正常專案常有數十個 session 檔），超限放棄回 None 不阻塞不外洩） | `EXCLUDED_NAMES`, `ItemResult`, `InstallPlan`, `plan()`, `install()`, `read_manifest()`, `transaction_id()`, `journal_path()`, `installed_nodes()`, `project_paths()`, `validate_landing_spots()`, `_open_journal_fd()`, `_parse_journal_nodes()` |
| `routes/restore.py` | `POST /api/restore/plan`（唯讀）：回 `{bundle, dest, dest_status}`——`bundle` 是**使用者送什麼就回什麼**（名字模式回名字、路徑模式回絕對路徑），前端拿它比對「換了包之後舊 plan 還在 state 裡」；body 的 `bundle`／`bundle_path` **二擇一**（票 11，走`restore.resolve_source_and_dest`）。**位置問題不是錯誤**（沿用 `routes/backup.py`）——一律 200 用 `dest_status` 旗標表達，錯誤碼只留給請求本身壞掉（`unknown_bundle`／`dest_invalid`／備份目錄三態）；`AppConfig.load()` 的 `ValueError` 轉 `config_unreadable` 500（JSON 剖析訊息不可當判別碼外洩）。實際展開走 `POST /api/sessions` kind=restore，不在此處。移機端點（票 03＋06＋07）：`GET /api/restore/project-paths`（唯讀，票 06：列包內專案的舊路徑〔讀歷史檔 cwd〕＋建議新路徑＋存在性）／`install-plan`・`install` 的 body 帶 `mapping:[{old,new}]`（驗證全在模組、判別碼入 400 白名單）／`POST /api/restore/adopt-config`（票 07：用備份包重建 config.json——**落點由使用者逐項確認後送回**、server 以 `install.validate_landing_spots` 全部重驗，成員必須是 manifest 宣稱的〔`unknown_account_key`／`unknown_extra_name`；這是輸入 hygiene **非 bundle 綁定**——config 落點授權的是目的地，install 對任意 bundle 一視同仁靠 no-clobber／containment／provenance（票 03 模型，Codex 票 07 R1 F1 措辭對齊）〕、同名重複拒、root 的 default_account 必須指向本次確認的帳號；與 onboard 共用 `create_if_absent`＋同一把 `_config_lock`〔spec §4.3.1〕，已存在 409 零寫入；accounts／extra 存 raw、不留 DEFAULT 帳號）／`POST /api/restore/install-plan`（唯讀預覽）／`POST /api/restore/install`（**本檔唯一寫入端點**，能力全在 `backup/install.py`；server 以相同輸入重算 plan〔ADR-0002，body `extra="forbid"`，塞 plan → 422 零寫入〕；落點從已落檔 config.json 讀不由前端送（**含 extra**：`plan(..., extra=config.extra)`，票 07 接線）；config 走 `AppConfig.load_existing()`（鎖內單次讀取、**永不 fallback**——exists→load 兩段式閘等鎖期間 config 被刪就退回 DEFAULT_CONFIG〔default=~/.claude〕，且 fallback 有檔案級與 accounts 欄位級兩層；不存在→`config_not_initialized` 400、壞 JSON／缺 accounts→`config_unreadable` 500；唯讀預覽維持 fallback 慣例）；與共通設置／範本部署共用同一把 `setup_lock`）；**錯誤分流**〔階段 10 守門〕：本檔三支端點共用 `_CONFIG_UNREADABLE`＝`(ValueError, TypeError, AttributeError, OSError)` 包住 `AppConfig.load*()` **那一行**〔壞 JSON／缺 accounts／頂層容器欄位畸形〔`roots:1`、`project_overrides:[]`〕／讀檔就失敗〔是目錄、沒權限〕全歸 `config_unreadable` 500，剖析訊息不可外洩、完整例外只進 log；**只包 load 那一行**——包大了會把模組自己的 TypeError 吞成「設定檔壞掉」掩蓋真 bug；**`FileNotFoundError` 是 `OSError` 子類**故 install 的 `except FileNotFoundError` 必須排在前面〔否則 `config_not_initialized` 被吞成 `config_unreadable`〕，有測試釘住順序；config 容錯本身的設計屬 `.scratch/config-resilience` 那張票，這裡只保證端點合約〕，模組拋的一律走 `_module_error` 照實回——清單內是使用者能修的輸入問題 400、其餘是環境問題 500〔如 `journal_unavailable`〕，**不冒充 `config_unreadable`**（誤報會讓使用者去修一個沒壞的檔案、前端也分不出該重新展開來源還是修權限）；`install()` 另把 `_require_source_identity` 的 `OSError` 正規化成 `source_not_a_bundle`〔與 plan 對同一情形同答案，否則裸穿成非合約 500〕 | `router` |
| `setup/install_specs.py` | 開發工具資料表＋allowlist 安裝命令：`ToolSpec`（id/label/tier/binary/version_argv/install_command/docs_url）常數表（macOS/Homebrew 生態，core＋recommended 兩級）；**安全不變式：安裝命令只能來自本檔常數、route 以 install_id 查表**，Homebrew 本體 install_command=None（僅手動複製指令） | `ToolSpec`, `TOOL_SPECS`, `get_spec()`, `get_install_command()` |
| `setup/env_detect.py` | 開發工具偵測純函式：which 命中才探版本（`_VERSION_TIMEOUT` 2s＋ThreadPool 平行、保序）；which/run 參數注入便於測試；版本探測失敗→version=None 不中斷；`ToolStatus` 另帶三個 UI 顯示欄位 `binary`（未安裝時沒有版本可列）／`install_command`／`manual_command`（＝spec 的 `note`，僅手動安裝者如 Homebrew），命令字串一律由後端資料表供給、前端不得自帶——露出字串不鬆動 allowlist（PTY 執行仍只吃 `install_id` 查表），反而讓畫面顯示與實際執行同源 | `ToolStatus`, `detect_tool()`, `detect_all()` |
| `setup/common_config.py` | 雙帳號共通設置（B/C 共用）：`build_account_graph`（模組自為安全權威，expand→absolute→resolve＋拒 home／祖先／根＋拒 target 解析到 source、與 source 祖先—子孫重疊、target 彼此重複或彼此巢狀）、`probe_entry`（lstat no-follow 十一態，含 source 型別前置驗證；`ok` 判定要求 realpath 落在 source dir 內）、`plan`（純函式）、`apply`（逐項盡力＋mutation 前重探測與 **雙側** TOCTOU 重驗〔target dir 走 realpath、source dir 走 `(st_dev, st_ino)` 身分比對 `Plan.source_identity`——前置閘驗過之後才被抽換的 source 判 `source_dir_moved` 停手，否則會建出字面指向 source 卻解析到未驗證內容的連結；**兩側都是縮小窗口而非消除**，比對到 mutation 之間仍是 check-then-act〕＋動作與授權需求由驗過的 state 重算＋`(account, entry)` 授權閘＋就地改名備份不覆蓋＋relink 走隔離改名不誤刪＋複製走 `safe_fs.copy_file_no_clobber`）；目錄同一性與巢狀判定走 `(st_dev, st_ino)`（APFS 預設不分大小寫，字串比對會把 `~/.claude` 與 `~/.CLAUDE` 當成兩個目錄）；`apply` 逐 op 驗確實屬於 graph；`OpResult.error` 是穩定判別碼（errno 映射，完整例外只進 log）；INFO 記 apply 起訖與備份位置、WARNING 記 conflict/stale/failed、ERROR 帶 traceback，不記檔案內容；source entry 是 symlink 時連字面路徑不追鏈；`repair`（C 移機還原專用：備份包存的是連結本身而非目標，還原後 target 的連結全指著舊機器絕對路徑）——拿**同一份 allowlist** 只重建 `broken_link`，其餘狀態一律 skipped 不動（**`wrong_link` 刻意不收**：它分不出「備份帶回的舊機連結」與「使用者刻意指到別處且仍有效的設置」，而 relink 不需 overwrite 授權，收進來就是免授權改寫後者——交給共通設置卡那條看得到逐項狀態才按套用的人工授權路徑；`missing` 沒有連結可修；`real_file`／`content_differs` 要 overwrite 授權）；**清單外的斷鏈不進 plan 故連判斷都不做**（可能是使用者自建）；source dir 不在／不是目錄／realpath 已非驗過的那個／讀不出內容 → 整批 `raise ValueError(<判別碼>)` 不回半套（逐項回 source_missing 只是把同一原因講六遍）；檔案操作**委派 `apply`（overwrite 空清單）**以繼承 apply-time 重探測、containment 重驗與 op-in-graph 檢查，不另開破壞性路徑 | `ENTRY_SPECS`, `build_account_graph()`, `probe_entry()`, `plan()`, `apply()`, `repair()` |
| `setup/safe_fs.py` | 檔案系統寫入原語（`common_config` 與 `templates` 共用）：`copy_file_no_clobber`（`O_EXCL｜O_NOFOLLOW` 不跟隨不覆蓋、選填 `dir_fd` 讓路徑不再經名稱解析、寫滿短寫、失敗比對 `(st_dev, st_ino)` 只清自己建的半截檔）、`write_bytes_atomic`（移機安裝路徑自用的原子發布：temp→fsync→link→unlink temp，`link` 的 EEXIST 是原子 no-clobber；最終名一出現即內容完整且不覆蓋既有目標、重跑收斂，**斷電下目錄項持久性由呼叫端 per-directory `fsync(dir_fd)` 承擔**〔spec §4.2.5 成本決策〕；本進程活著走到失敗路徑都清自己的暫存檔（**link→unlink 間被硬中斷則殘留且重跑不清**——別的進程留下的殘骸由呼叫端 `find_stale_temps` 指認後回報給使用者，**不自動刪**，票 08 R3）；暫存名含 PID＋隨機段防並行相撞）、`find_stale_temps`（暫存殘骸**指認，唯讀不刪**，票 08：SIGKILL／斷電落在 link 前後留下的暫存檔，重跑的 PID＋隨機名對不上、`write_bytes_atomic` 的失敗清理也涵蓋不到；**為什麼只回報不刪**〔Codex 票 08 R3〕——可拿來授權刪除的只有檔名前綴、檔名內 PID 已不存在、型別是一般檔，三者**全是可預測可偽造的檔名特徵**，證明得了「名字像我們建的」證明不了「確實是我們建的」，達不到專案對破壞性操作的底線「只刪自己建的」，故刪除授權交給看得到完整路徑的使用者〔Plan B 的 UI〕；判準只用來指認嫌疑、不授權任何刪除，誤判兩個方向都只是回報多了或少了。只認自己的命名空間、只認產生者**確定**不在的〔`ProcessLookupError`＝ESRCH 是唯一「確定不在」的訊號，`EPERM` 與其餘問不出來的一律不指認〕、只認一般檔〔同名目錄或 symlink 不是我們產生的〕。liveness 是單機語意：跨 PID namespace 共享檔案系統時 `kill(0)` 對別 namespace 的活 writer 回 ESRCH，當前部署〔macOS 桌面 app、sidecar 由 Tauri 殼直接 spawn〕沒有這個情形）、`error_code`（errno→穩定判別碼，含 macOS 對 `O_DIRECTORY｜O_NOFOLLOW` 開到 symlink 回的 `ENOTDIR`、不支援 hard link 的檔案系統回的 `ENOTSUP`／`EOPNOTSUPP`） | `copy_file_no_clobber()`, `write_bytes_atomic()`, `find_stale_temps()`, `error_code()` |
| `setup/templates.py` | 範本部署：`TEMPLATE_SPECS` allowlist（含 `source_class`，build 分離的唯一真實來源；label/description 一律英文，B-4 以 id 對前端 i18n catalog）、`templates_root`（env 覆蓋＞凍結資料目錄＞repo public seed）、`build_manifest_entries`／`load_manifest`（**產生端與載入端各套一份**拒絕規則：symlink／絕對路徑／`..`／非 file-dir／重複 path／子項的目錄祖先未宣告／空 manifest；載入端另以 `lstat` **與實體種子樹對帳**——宣告不是證據，且輸出正規化成父在子前）、`resolve_destination`（正規化＋home 本身/祖先/根防呆，ADR-0001 取向）、`probe_entry`（`lexists`/`islink` no-follow 三態＋父目錄 realpath containment，走 inode 身分）、`plan`（唯讀預覽、conflict 停 subtree、blocked ancestry 逐元件上溯不用字串 prefix）、`deploy`（root fd 身分驗證＋每層子目錄 `O_DIRECTORY｜O_NOFOLLOW` 從父 fd 開出＋檔案以 `dir_fd` 寫入、只建目的地最後一層、寫入前 fd 相對重探、永不覆蓋、逐項盡力、穩定判別碼、INFO/WARNING/ERROR 稽核 log） | `TEMPLATE_SPECS`, `TemplateSpec`, `ManifestEntry`, `MANIFEST_FILENAME`, `templates_root()`, `get_template_spec()`, `build_manifest_entries()`, `load_manifest()`, `resolve_destination()`, `probe_entry()`, `plan()`, `deploy()` |
| `setup/template_manifest.py` | artifact manifest（build／CI 對帳，runtime 不依賴）：`stage_templates`（來源樹先整棵盤點，symlink／非常規物件／id 不在 allowlist／分類與來源目錄不符一律**直接失敗**——不能交給 `rsync --no-links`，它遇 symlink 只印警告卻 exit 0）、`build_artifact_manifest`（分類取自 `TEMPLATE_SPECS`、**全部**範本都列、逐檔 sha256、目錄也登記）、`verify_artifact`（雙向對帳＋逐檔重算雜湊＋staged 根目錄白名單；fail-closed：`source_class` 與 allowlist **相等**比對、schema 驗證排在目錄判斷之前、缺席的 id 視為違規、`included=false` 連空目錄都不許存在；只回違規清單不拋例外） | `ARTIFACT_MANIFEST_FILENAME`, `stage_templates()`, `build_artifact_manifest()`, `verify_artifact()` |

### src/ — React 前端
| 檔案 | 職責 | 匯出 |
|------|------|------|
| `App.tsx` | 根元件：組裝 Sidebar+TabBar+多 Terminal、**啟動走 `runStartup`＝`bootstrap` 回傳值的唯一消費路徑**（不變式：初次掛載的 effect 與 Splash 的 `onRetry` 都必須經過它——曾讓 Splash 直接呼叫 `bootstrap`，結果重試成功時 `firstRun` 沒人接、淡出到還沒設定過的主畫面，`projectsError` 也沒人接）、持有 `splashDone` 掛載 `<Splash>`、`projectsError` warning banner（專案掃描失敗非致命，附「重新掃描」）、5s health poll→backendStatus、後端斷線 banner+重啟原子轉移、claude/權限 notice、快捷鍵（Cmd+W/1-9/,/T/R；**`!splashDone` 的 gate 必須放在 `preventDefault()` 之後**——放 handler 開頭直接 return 會讓事件落回 Tauri 預設，Splash 期間 Cmd+W 直接關掉整個 app）、掛 Settings（含 `onRerunOnboarding`：關設定頁→開精靈）/Picker/Onboarding、關閉執行中分頁確認框（CloseConfirm）、useLayoutEffect 同步 `modalOpen`（任一 modal 開啟＝唯一寫入者，供拖檔 drop gate）；pendingTitle 用 `isLiveClaudeTab`（共用判定）；加掛 `<DndContext>`（PointerSensor distance:5、pointerWithin、onDragStart/End 依 data.type 分流 tab 排序/path 貼路徑、DragOverlay）+ module 層 `dropPathsToActiveTerminal`（drop gate：modal/kind/status/IME/paths/格式化） | `App` |
| `store/useAppStore.ts` | Zustand 單一真相源：**啟動序列 `bootstrap`**（port→token→health→config→專案；三條契約缺一不可：①**不得宣告為 `async`**——`async` 的 `return inFlight` 會產生採納結果的新 promise、identity 去重失效；②進行中回同一個 promise 物件而非 null，兩個呼叫端才拿到相同答案；③settle 後只在 `inFlight === p` 時清除，否則重試會撿到上一輪的舊 null 而永不 restart。`startup: running|ready|failed` + `startupError`〔技術原文，只餵 Splash 詳細資訊與 console〕；`restart` 分支**必須在 `await restartSidecar()` 之前就把 startup 切成 running**——它在 Rust 端同步等最長 30s，晚切會讓 Splash 整段期間顯示「可按的重試」而非 disabled 的「重試中…」；`loadProjects` 失敗走局部 catch、由回傳的 `projectsError` 帶給 App，不落進統一 failed handler）+ tabs/activeTab/projects/config + session 生命週期（模式 A）+ config actions + 韌性（Tab.status offline/ended、backendStatus 狀態機、setTabStatus/restartTab/markAllTabsEnded/recordHealth；`setTabStatus`／`setTabActivity` 對不存在的 tabId 原樣返回，不建新 `tabs` 陣列——精靈的安裝終端機走合成 tabId，照 set 會讓 TabBar／Workspace 白重繪）+ `Tab.activity`（working/idle 活動態）+ setTabActivity + 關閉守門（requestCloseTab：ready+sessionId 一律跳確認框〔含 idle〕、offline/ended 直接關、pendingCloseTabId）+ `modalOpen` 旗標（拖檔 drop gate 單一判定來源）；`Tab.kind`（claude/terminal/dashboard/memory）；openTab 以 (path,account) 識別（terminal 不去重）；openTab 加選填 forceNew（claude 跳過去重、強制開新視窗；右鍵「開新 Claude 視窗」與 restartTab 使用）；`openMemory` 單例分頁（比照 openDashboard，純前端無 session；closeTab/restartTab/markAllTabsEnded 與 dashboard 同組免特判 session）；requestCloseTab 改用 `isLiveClaudeTab`；restartTab 沿用 `tab.kind`；加 `reorderTabs` action；`openTab` 的 terminal 分頁用 `insertTabAdjacent` 相鄰插入 | `useAppStore`, `Tab` |
| `lib/sidecar.ts` | 拿 port/token、HTTP client wrapper（所有 fetch 帶 `X-Fledge-Token`，`sidecar.test.ts` 列舉鎖定）；`waitForSidecarPort` 每輪一併查 `sidecar_spawn_error`，有值即帶原文**立即失敗**（spawn 失敗時 port 永遠不會來，空等 30s 只是把精確原因換成通用逾時訊息）；`fetchConfig` 過 `appConfigGuard` 才回傳（畸形 config 擋在這層，否則啟動被判成功、Splash 淡出後才在 Sidebar／設定頁崩潰）；`scanPreview` 回 `{path,count,status}`、`checkDir` 回 `DirStatus`；`wsUrl` 附 `?token=`；`createSession(port, opts)` 走 options 物件（`path`／`account`／`kind`＝claude·terminal·install·login／`installId`／`loginTarget`），body 只放有值的欄位——**安裝一律只送 `install_id`、不送 account、永不送命令字串**（spec §5 allowlist 不變式）；失敗擲 `SessionError`（帶 400 的英文判別碼 `code` 供呼叫端映射 i18n，message 維持含狀態碼的既有格式）；記憶層 fetchers（`fetchMemoryOverview` 含 `Array.isArray(projects)` 守衛/`fetchMemoryRelated`/`fetchMemoryItem`〔!ok throw〕+ links 寫入 wrappers `memoryJson`〔!ok throw，支援右欄錯誤 UX〕）+ `putKmsRoot`；`AppConfigData.kms_root?`；加 `fetchDirTree`（HTTP !ok throw、200 帶 status 不 throw）+ 型別 `DirEntry`/`DirTreeResult`；加 `fetchSetupStatus`（回 `ToolStatus[]`，!ok throw 讓卡片顯示偵測失敗）；共通設置 `commonConfigPlan`／`commonConfigApply`（走共用的 `setupPost`，失敗擲 `SetupError`＝帶判別碼 `code`、message 不含判別碼；`apply` 的 `overwrite` **必填不給預設**，讓「精靈送空清單」是呼叫端的顯式表態）+ `COMMON_CONFIG_ENTRIES`（前端持有的 entry 清單，**不含進階項 `projects`**；後端 allowlist 仍是權威，不在清單內回 `unknown_entry`）+ state/action/outcome 三組 union 型別；範本三支 `fetchTemplates`／`templatesPlan`／`templatesDeploy`（同走 `setupPost`／`SetupError`；`fetchTemplates` 回的每筆帶 `available`，未內建者照列由 UI 決定呈現）；備份 `fetchBackupStatus`／`putBackupDir`（**後者回 `{"error":code}` 而非 kms-root 那種 `detail` prose**，判別碼要能被 i18n 映射；失敗擲 `BackupError`，`code` 只放欄位不進 message）；還原 `restorePlan`（回 `{bundle,dest,dest_status}`，失敗擲 `RestoreError`）＋`commonConfigRepair`（走 `setupPost`，**無 `overwrite` 參數**）＋`RESTORE_REPAIR_ENTRIES`（＝`COMMON_CONFIG_ENTRIES` 再加 `projects`——移機後那條連結同樣是斷鏈，不送就永遠修不回來，正是後端把它收進 allowlist 的理由）＋`CreateSessionOptions.restoreBundle`／`restoreDest` | `…既有…`, `fetchMemoryOverview()`, `fetchMemoryRelated()`, `fetchMemoryItem()`, `confirmSuggestion()`, `dismissSuggestion()`, `addMemoryLink()`, `removeMemoryLink()`, `putKmsRoot()`, `MemoryOverview`, `MemoryItem`, `MemoryRelated`, `MemorySuggestion`, `fetchDirTree()`, `DirEntry`, `DirTreeResult`, `fetchCodexUsage()`, `CodexUsage`, `fetchSetupStatus()`, `ToolStatus`, `SessionError`, `CreateSessionOptions`, `SessionKind`, `commonConfigPlan()`, `commonConfigApply()`, `COMMON_CONFIG_ENTRIES`, `SetupError`, `CommonConfigPlan`, `CommonConfigOperation`, `CommonConfigOpResult`, `CommonConfigRequest`, `CommonConfigState`, `CommonConfigAction`, `CommonConfigOutcome`, `fetchTemplates()`, `templatesPlan()`, `templatesDeploy()`, `TemplateInfo`, `TemplatePlan`, `TemplateFileOp`, `TemplateFileResult`, `TemplateDeployResult`, `TemplateState`, `TemplateFileState`, `TemplateOutcome`, `DEFAULT_ACCOUNT_KEY`, `fetchBackupStatus()`, `putBackupDir()`, `BackupStatus`, `BackupBundle`, `BackupError`, `restorePlan()`, `RestorePlan`, `RestoreDestStatus`, `RestoreError`, `commonConfigRepair()`, `RESTORE_REPAIR_ENTRIES` |
| `lib/dialog.ts` | Tauri plugin-dialog 封裝：開系統資料夾選擇器 | `pickDirectory()` |
| `lib/clipboard.ts` | 剪貼簿寫入（`navigator.clipboard.writeText`）：失敗只記 log 不擲回，回傳成功與否讓呼叫端決定要不要提示（終端機複製 fire-and-forget、設置卡要顯示「已複製」） | `writeClipboard()` |
| `lib/useCardSession.ts` | 設置卡共用的「卡片內單一終端機 session」生命週期 hook（環境卡的安裝、登入卡的 OAuth）：一次只跑一個、切換時**先 `await closeSession` 關完舊的才建新的**（後端一收到 create 就 spawn PTY，先建後關會讓兩者真的併行，票 24 Codex R1）、卸載一律收 PTY 不留 orphan；`runningRef`／`portRef` 走 render body 同步（cleanup effect 空依賴，不能跟著 port／running 重跑）、`mounted` 在 mount effect 內重設（StrictMode 會 mount→cleanup→再 mount）；`start()` 回傳是否真的建立，呼叫端據此收起過場 UI；錯誤走 `mapError(code)`→`fallbackError()`，判別碼不得直接顯示、例外原文只進 console（spec-b4 §5；`fallbackError` **刻意不收 reason**——沒有位置放它，就不會有人再把 `String(e)` 插回畫面） | `useCardSession()`, `CardSession` |
| `lib/sourceHygiene.test.ts` | 原始檔衛生防線：掃 `src`／`sidecar`／`src-tauri/src` 的 `.ts/.tsx/.css/.json/.py/.rs`，斷言不含 NUL 或其他非預期控制字元（僅允許 `\t`／`\n`）。起因是票 25 曾把真正的 NUL 寫進 `LoginCard.tsx`，git 判成二進位後**該 commit 的新增完全沒進 diff**，而本專案的審查流程（對抗式審查、兩軸 review、PR diff）全建立在 diff 上。用 `import.meta.glob` 而非 `node:fs`（無 `@types/node`）；另有一條「掃得到檔案」的哨兵測試，避免 glob 失效後空掃永遠綠 | — |
| `lib/appConfigGuard.ts` | 純函式：啟動時的 config 形狀驗證，`fetchConfig` 套用。**界線是「會不會 throw」而非「欄位齊不齊」**——容器形狀（record vs array）與元素物件性必須驗（`a.config_dir` 對 `null` 存取才是真正的崩潰源），具名欄位**只在存在時**驗型別、**一律不得要求必須存在**。後者是 Codex PR-gate 的修正：曾要求 `accounts[*].label` 必存，但 `sidebarGroups.ts:85` 的 `meta?.label \|\| key` 正是為缺席而設的 fallback、後端 `app_config.py:103` 也原樣收下不補欄位，缺 label 的歷史 config 本來就能用——判它致命會讓使用者**永久卡在啟動畫面**（重試讀回同一份檔案救不了），比原本要防的崩潰更糟。`subscriptions`／`kms_root` 另為選用欄位（未提供合法、提供則型別須對）。刻意不引 zod：淺層檢查手寫足夠 | `isUsableConfig()` |
| `lib/liveTab.ts` | 純函式：live Claude session 判定（kind=claude + ready + sessionId），store/App 共用；terminal tab 永遠回 false | `isLiveClaudeTab()` |
| `lib/backupFormat.ts` | 純函式：`blockingReason`（備份卡的阻斷原因決策鏈——旗標**可能同時成立**，取第一個成立的、只顯示它；順序與後端擋下的順序一致，且 containment 排在目錄狀態**之前**：位置選錯時把那個目錄建出來也沒用。映射走**顯式表而非動態組 key**，否則後端多一個狀態就會把 i18n key 原文印到畫面上）＋`freshnessLevel`（≤7 fresh／≤30 stale／其餘 overdue；**從未備份與超過一個月同級**，兩者都代表現在沒有保護）＋`formatSize`（停在 GB）＋`formatBundleTime`（**刻意不顯示秒**：時間戳來自檔名、精度只到分鐘，印出秒等於宣稱一個沒有的精度） | `blockingReason()`, `BlockingReason`, `freshnessLevel()`, `Freshness`, `formatSize()`, `formatBundleTime()` |
| `lib/restoreFormat.ts` | 純函式：`restoreBlocking`（還原卡的阻斷決策鏈：未設定→目錄不可用→沒有備份包→缺還原腳本→缺 python3。**`containment` 刻意不在鏈上**，與後端 `resolve_backup_dir` 一致；**只看 `restore_script_available`**——備份腳本缺席不影響還原）＋`destMessageKey`（展開位置狀態→catalog key，`Record` 涵蓋整個 union，後端加狀態會編譯失敗）＋`repairScope`（source＝第一個登記帳號，與共通設置卡同一慣例；少於兩個帳號回 `null`，不拿必然的 `empty_targets` 去問後端） | `restoreBlocking()`, `RestoreBlocking`, `destMessageKey()`, `repairScope()` |
| `lib/tabOrder.ts` | 純函式：`reorderTabs`（拖曳排序 arrayMove）+ `insertTabAdjacent`（需求 2 相鄰插入：同 projectPath 群尾） | `reorderTabs()`, `insertTabAdjacent()` |
| `lib/tabTitle.ts` | 純函式：分頁顯示標題（claude 同 (path,account) 多視窗加序號 #2/#3，render 時算、關閉重編號；非 claude 回 raw title） | `displayTabTitle()` |
| `lib/terminalRegistry.ts` | 終端機 imperative handle 旁路登錄（比照 activityTracker）：`Map<tabId, {paste, isComposing}>`，供 dnd drop 找 active 終端機貼路徑 | `registerTerminal()`, `unregisterTerminal()`, `getTerminal()`, `TerminalHandle` |
| `lib/memoryView.ts` | 純函式：記憶面板 overview→視圖（`buildGroups` 分組／`defaultExpanded` 專案展開／`matchesFacet`+`applyFacet` 前端過濾／`selectionValid` path-anywhere 含 fan-out）+ 型別 `MemoryGroupVM`/`Facet`/`Selection` | `buildGroups()`, `defaultExpanded()`, `matchesFacet()`, `applyFacet()`, `selectionValid()` |
| `lib/sidebarGroups.ts` | 純函式：依帳號分組 + 三 band 分類/排序（開啟中/已接觸/自動發現）；每群組 recent band 最多 `RECENT_BAND_LIMIT`(=3) 筆（manual 豁免）、溢出存 `AccountGroup.surfacedMore`（Sidebar 呈現邏輯，可單元測試） | `groupProjectsByAccount()`, `tabKey()`, `AccountGroup`, `RECENT_BAND_LIMIT` |
| `lib/wsReconnect.ts` | 純函式：WS 重連策略（4001/1008 不重連、其他 backoff 5 次） | `shouldReconnect()`, `nextDelay()`, `MAX_RECONNECT_ATTEMPTS` |
| `lib/backendStatus.ts` | 純函式：backend health 狀態機（up/suspect/down/restarting + up-hysteresis） | `nextBackendState()`, `BackendStatus`, `BackendState` |
| `lib/activityTracker.ts` | PTY 活動偵測：per-tab 閒置計時器 + 轉換節流（IDLE_MS=2000，離開 working 的確認窗，容忍思考暫停、防 working↔waiting 閃爍）+ **size-gate（MIN_OUTPUT_BYTES=16，忽略 idle 游標心跳等小 chunk；打字整行重繪 >16 無法濾，屬接受限制）**，由 Terminal.onmessage 餵（含 chunk size）、標記 `Tab.activity`（best-effort working/idle） | `recordActivity(tabId,size)`, `clearActivity()`, `IDLE_MS`, `MIN_OUTPUT_BYTES` |
| `lib/tabDotState.ts` | 純函式：`Tab` 的 status × activity → 單一 `DotState`（status 優先；給 TabBar 狀態點用） | `tabDotState()`, `DotState` |
| `lib/imeReplayGuard.ts` | 純狀態機：IME 切視窗重放攔截保險網（組字中文字 onData＝候選 → compositionend 空＋blur 確認武裝 → 攔相同 trusted insertText 恰一次）＋組字中 Meta keydown 該吞判定（真懸置核心——防 xterm 提前 finalize） | `ImeReplayGuard` |
| `lib/imeDraftTracker.ts` | 純狀態機：IME 懸置幽靈草稿該不該顯示（compositionupdate 記草稿 → end 空＋blur 確認顯示 → compositionstart/dismiss 退場），渲染在 Terminal 接線 | `ImeDraftTracker` |
| `lib/flowControl.ts` | 純狀態機：PTY 輸出 watermark flow control（`record` 計入/`ack` 扣除 → 越 HIGH=100K 回 pause、回落 LOW=10K 回 resume，遲滯+冪等；`pendingBytes` 供驗收量測），由 Terminal.onmessage 計帳、觸發送 WS 控制訊息 | `FlowController`, `FlowSignal`, `HIGH_WATERMARK`, `LOW_WATERMARK` |
| `lib/dropPath.ts` | 純函式：拖檔路徑智慧引號格式化（safe charset `[\p{L}\p{N}/._-]` 原樣、其餘 POSIX 單引號跳脫 `'`→`'\''`、含控制字元整項跳過、多檔空白 join+尾隨空白、全跳過回空字串），供 Terminal 拖檔接線 | `formatPathsForPaste()` |
| `lib/terminalLinks.ts` | 純函式：終端機行內連結偵測（Cmd+click 開連結用）。`linksInLine` 掃 URL（只放行 http/https/mailto/tel、剝尾隨標點）+ 檔案路徑（相對路徑對 projectPath 解析、`~/` 展開、剝 `:line:col`、containment 落在 home 內才放行、`..` 逃逸/home 外拒絕；無 home 只回 URL）；`strIndexToColumn` 把字串索引→xterm 欄位（全形字 spacer 校正） | `linksInLine()`, `strIndexToColumn()`, `TerminalLink`, `LinkOpts` |
| `i18n.ts` | i18n 最小基建（react-i18next）：語言權威序 localStorage `fledge-lang`（讀取包 try/catch，Storage 拋錯退回 OS 偵測——這行在 module 層跑，拋出去＝app 開不起來）> OS zh 偵測 > en；**localStorage 是唯一權威、不是快取——`config.ui.language` 不存在也不會有（2026-07-26 裁定，`docs/planning/i18n-design.md` §3 已改寫）**；defaultNS dashboard；註冊 namespace `dashboard` + `memory` + `sidebar` + `onboarding` + `splash` + `app`（**`defaultNS` 雖是 `dashboard`，但那是「用量儀表板」的 namespace 不是通用 app 命名空間**——app 層字串請進 `app`）；init 與 `languageChanged` 同步 `document.documentElement.lang`（`index.html` 是靜態 `lang="en"`） | default `i18n` |
| `locales/{zh-TW,en}/dashboard.json` | 數據儀表板 locale catalog（兩檔 key 對齊，`i18nCatalog.test.ts` parity 鎖定）；既有元件字串遷移留後續 | — |
| `locales/{zh-TW,en}/memory.json` | 記憶層 locale catalog（tabTitle/entry/search/section/related/attribution/settings/detail/a11y…；`memory-parity.test.ts` 鎖兩語 key 一致；type enum 值刻意顯示原值不翻譯〔§4.6.13 記錄例外〕）| — |
| `locales/{zh-TW,en}/sidebar.json` | 側邊欄/分頁/檔案樹 locale catalog（search/menu/empty/tree/tabBar；`sidebar-parity.test.ts` 鎖兩語 key 一致） | — |
| `locales/{zh-TW,en}/splash.json` | 啟動畫面 locale catalog（`wait.hint`／`errors.startup_failed`／`errors.startup_fix`／`actions.retry`·`retrying`·`details`）。錯誤群刻意用 `errors.` 前綴以繼承 parity 的「不得帶 `{{reason}}`」規則；本 ns 文案**全部無插值**，例外原文只進「詳細資訊」與 console | — |
| `locales/{zh-TW,en}/app.json` | app 殼層 locale catalog（`banners.projects_failed`／`banners.rescan`）。既有兩條 banner（claude 未安裝／權限）仍是硬編中文，屬 i18n 改造的既有債、另案處理 | — |
| `locales/splash-parity.test.ts` | `splash` 與 `app` 兩 ns 共用的 parity 鎖（`describe.each`）：兩語 key 集合一致、無 inline style/`<span>`、`errors.*` 不得帶 `{{reason}}`、同 key 的插值與標籤兩語一致 | — |
| `locales/{zh-TW,en}/onboarding.json` | 引導精靈與設置卡 locale catalog（common/welcome/roots/env/login/cc/sys/done/st/results/errors 十一群共 160 key；文案取自 `docs/design/b4-onboarding-wizard.html` 的 `T` 物件，`errors.*` 與 `results.*` 為 spec-b4 §5 約定的判別碼群〔key 用判別碼原名，`cc.state.*`／`sys.state.*`／`sys.result.*` 同此約定；範本的逐檔結果**不共用 `results.*`**——那組措辭是共通設置的「已建立連結」，對範本是錯的〕，`sys.tpl.*` 以**範本 id** 為 key（demo 的位置式 `tpl1`–`tpl3` 已汰換，後端 `label`／`description` 只在 catalog 沒有該 id 時當 fallback）；demo 的 `cc` 四條現況文案只覆蓋靜態示意的 4 個狀態，票 26 改成窮舉後端 11 個 state 的 `cc.state.*`）。精靈的三段品牌字串（`Fledge` wordmark／tagline／`Built for Claude Code`）刻意不進 catalog、兩語同顯英文，屬 §4.6.13 記錄例外（demo 同樣未給 `data-t`）。依 i18n-design §2.3 只放語意標記與插值，樣式一律留在 CSS——`<Trans>` 標籤僅 `<strong>`/`<code>`/`<br />`（basic nodes，見 `i18n.ts`）與具名 `<rerun>`（票 29 映射成按鈕）；插值變數 `{{source}}`/`{{target}}`（帳號）、`{{path}}`/`{{name}}`（路徑或檔名）、`{{count}}`/`{{projects}}`/`{{roots}}`/`{{accounts}}`（計數）。`onboarding-parity.test.ts` 鎖四件事：兩語 key 集合一致、無 inline style/`<span>`、同 key 的插值與標籤兩語一致、`errors.*` 不得帶 `{{reason}}`（例外原文與後端判別碼只進 console——插值一旦存在，呼叫端就會有地方塞 `String(e)`） | — |
| `lib/usageFormat.ts` | 純函式：儀表板數字格式化（fmtUSD 含負數/<$0.01、fmtPct、fmtTokens K/M、fmtClock HH:mm、fmtDayClock 今天/昨天/M/D）＋Codex 額度窗口標籤角色判定（`codexWindowLabel`：依 window_minutes 非位置，invalid 退 fallback） | `fmtUSD()`, `fmtPct()`, `fmtTokens()`, `fmtClock()`, `fmtDayClock()`, `codexWindowLabel()`, `WindowLabel` |
| `lib/usagePoll.ts` | 純函式：儀表板輪詢 gating（作用分頁＋頁面可見才打 API）＋30s 成本面板間隔／15min Codex 額度間隔常數 | `shouldPoll()`, `POLL_INTERVAL_MS`, `CODEX_USAGE_INTERVAL_MS` |
| `lib/dashboardLogic.ts` | 純函式：面板邏輯（donutParts top4+rest 角度） | `donutParts()` |
| `lib/subscriptionsForm.ts` | 純函式：訂閱費表單驗證（與 sidecar PUT 400 規則一致：name 非空、cost 有限非負）＋`dropBlankRows`（名稱與費用**都**空白的列＝按了新增又沒填，濾掉；只填一欄的留給驗證擋，不默默丟）＋`sameSubscriptions`（含順序比對，供「沒改就不送」判定；空 vs 有內容回 false 故「刪光」仍算改動） | `validateSubscriptions()`, `dropBlankRows()`, `sameSubscriptions()` |
| `lib/onboardingSteps.ts` | 純函式：引導精靈頁面序列，依 `mode` 分兩條路——**全新設定**（七頁；帳號數 <2 時整個 `common` 頁不出現＝六頁）與**移機** `welcome→bundle→roots→paths→install→env→login→repair→done`（刻意無 `common`／`system`：共通設置的連結是跟著備份搬回來的、要的是 repair 而非重建，範本是給新使用者鋪底的〔上游 spec §5.1〕；帳號數不影響本序列；備份包裡沒有專案歷史時整個 `paths` 頁不出現，`hasProjectHistory` 未給＝包還沒展開，預設先留著）＋**身分定位** `stepIndex()`（導覽 state 存 `WizardStep` 而非索引：移機序列的 `paths` 去留是**非同步**得知的，使用者可能已走到 `install` 才因遲到的包資訊而增刪，存索引會讓同一個數字指到別頁——含 `paths` 時 `install` 是 4、不含時 4 是 `env`〔Codex F1〕；目前這一頁自己被移除時退到完整序列中它前面最近的、仍存在的一頁，往前退不跳過未確認的步驟）＋索引夾取（只擋兩端越界，語意定位不歸它管）＋進度條格數與填色（0-based，填到目前頁為止） | `wizardSteps()`, `stepIndex()`, `clampStepIndex()`, `progressCells()`, `WizardStep`, `WizardMode`, `WizardStepsInput` |
| `components/FileTree.tsx` | 檔案樹容器：lazy 載入快取（`Map<path,{entries,status}>`）+ 展開 Set + 錯誤節點 retry；遞迴渲染子層 | `FileTree` |
| `components/FileTreeNode.tsx` | 單節點（資料夾/檔案）：caret、icon、`useDraggable`（id `tree:`、data type=path）、雙擊 `openPath` 開檔 | `FileTreeNode` |
| `components/Dashboard.tsx` | 數據儀表板分頁（單頁長滾動）：成本面板 30s 輕輪詢（首掃 2s、visibility/isActive gating、error 保留舊資料標 stale）＋ KPI 5 卡/每日成本堆疊長條/模型 donut/時段 heatmap 週一起/專案表 top30；Codex 額度卡 `CodexPanel` 走**獨立 effect**（`/usage/codex` 實時：開啟即抓、開著每 15 分鐘、isActive→true 重開強制重抓、沒看不抓；失敗顯 unavailable/reauth 不端舊數字）；全手刻 SVG/CSS 零圖表依賴、字串全走 t() | `Dashboard` |
| `components/Sidebar.tsx` | 依帳號（＝專案類型）分組列專案 + 三 band（開啟中/已接觸/自動發現收合）+ 帳號色塊標題 + 底部開資料夾 + 右鍵選單（改帳號/Finder/移除/使用終端機開啟）；open/active 判定只認 claude tab；溢出展開器（surfacedMore）；收合窄軌（`localStorage fledge.sidebarCollapsed`）；搜尋列旁 dashboard(`ChartColumn`)+memory(`Brain`→`openMemory`) 入口（收合軌與展開列雙處）；專案列抽 top-level `ProjectRow`（`useDraggable` id `project:`、展開 caret 切換檔案樹、條件渲染 `<FileTree>`）；既有字串改走 `t()`（sidebar ns） | `Sidebar` |
| `components/TabBar.tsx` | tab 列：切換 + 帳號 chip + 關 tab + 統一狀態點（`tabDotState`：working 呼吸/waiting 穩定/連線態，取代 ●/⚠ 前綴）；terminal 分頁以 SquareTerminal 圖示取代狀態點；memory 分頁以 `Brain` 圖示 + 標題走 `memory:tabTitle`；分頁改 `SortableTab`（dnd-kit `useSortable` 水平排序；close button `onPointerDown` stopPropagation 不啟動拖曳）；aria 走 sidebar ns；標題改用 displayTabTitle（claude 多視窗序號） | `TabBar` |
| `components/Terminal.tsx` | xterm.js 渲染：連 WS（用 `wsUrl()` 帶 `?token=`）雙向 I/O + ResizeObserver 回報 PTY 尺寸 + onclose 重連狀態機（4001 ended/其他 backoff，gate on backendStatus 用 getState 不放 effect 依賴）；onmessage→recordActivity、teardown→clearActivity（活動偵測旁路，§7 僅 3 處）+ flow control 計帳（per-connection `FlowController`：write 前 record/write callback ack，越 HIGH 送 pause、回落 LOW 送 resume，控制訊息綁該連線 socket 不引用外層 ws）；回前景/切 tab 強制重繪（visibilitychange/focus/isActive → fit+refresh，rAF coalesce、dims 變動才回報 resize）+ WebGL renderer（active-only 掛載、context loss/載入失敗全域退 DOM 並 console.warn、`ENABLE_WEBGL` kill switch、fonts.ready 清 atlas）+ smoothScrollDuration 125/scrollback 5000 + IME 真懸置（container capture 吞組字中 Meta／Unidentified keydown 防 xterm 提前 finalize（Unidentified＝CapsLock 中英切換附隨事件、治組字中重複輸入）；`ImeReplayGuard` 重放保險網＋`ImeDraftTracker` 幽靈草稿 ghost DOM 掛 .xterm-helpers；切回後 Esc/點擊＝確認文字屬已知平台差異；`Terminal.css` 蓋 composition-view 為主題色＋底線）+ 拖檔貼路徑（整窗 `onDragDropEvent`、isActive 閘控、drop gate：!disposed/!modalOpen/ready/paths>0/格式化非空 → `term.focus()`+`term.paste(formatPathsForPaste)`，走既有 onData→ws guard；`isActiveRef` 改 render body 同步賦值消 gap）；選填 `onEnded`（**僅 PTY EOF／WS 4001 觸發，不含放棄重連**——那時命令可能還在跑，說它結束是謊報）供設置卡用：卡片的合成 tabId 在 store 內沒有對應 tab，`setTabStatus` 是 no-op，卡片因此收不到任何結束訊號；走 ref 不進 effect 依賴（呼叫端每 render 換引用，放進去會 teardown+rebuild xterm 毀掉終端機內容）；mount 註冊 `{paste, isComposing}` handle 到 terminalRegistry、unmount 註銷；`composingRef`（IME 組字旗標）；OS 拖檔 `onDragDropEvent` 加 IME gate＋選取複製（xterm 選取是內部狀態非 DOM Selection：Cmd+C capture 攔截 preventDefault 消 NSBeep／右鍵自訂 `ContextMenu`〔複製·貼上·全選〕，複製走 `term.getSelection()`＋`lib/clipboard` 的 `writeClipboard`、貼上走 `pasteFromClipboard`〔readText 全程 try/catch〕）＋Cmd+click 開連結（iTerm2 風格 `registerLinkProvider`：修飾鍵閘控〔Mac=Meta/其餘=Ctrl〕，未按住不提供連結＝不奪點擊；URL→`openUrl`、檔案路徑→`revealItemInDir`；連結偵測走 `lib/terminalLinks`，行號/全形字校正；接 `projectPath` prop 解析相對路徑；activate 二次確認修飾鍵；單行偵測，wrapped 連結為已知 v1 限制） | `Terminal` |
| `components/Settings.tsx` | 設定頁 modal（Cmd+,）：roots/manual 編輯（打字或「瀏覽…」picker）+ 帳號編輯（接 AccountsEditor）+ KMS 根目錄欄位（input+picker+儲存→`putKmsRoot`，字串走 `memory:settings.*`、含 saveError）+ header 掛 `<LangSwitch>` + body 尾端掛 `<DevEnvSection>`（票 29；`onRerunOnboarding` 由 App 注入）與 `<BackupCard>`（備份票 02）、`<RestoreCard>`（還原票 09，接在備份卡下面、吃 `config.accounts` 供斷鏈修復判角色） | `Settings` |
| `components/ContextMenu.tsx` | 通用右鍵選單（邊緣 clamp、任意鍵關、`MenuItem.disabled` 灰階常駐不可點） | `ContextMenu`, `MenuItem` |
| `components/ProjectPicker.tsx` | 選專案 dialog（Cmd+T，fuzzy filter） | `ProjectPicker` |
| `components/Splash.tsx` + `Splash.css` | 啟動畫面（webview 內滿版 overlay，z-index 500 高於 onboarding 的 400）：介面只有 `onDone`／`onRetry` 兩個 callback，**不自己呼叫 `bootstrap`**（它不知道 `BootstrapResult` 要怎麼消費，那是 App 的職責）。淡出只有一條規則＝自掛載起滿 2s 且 `startup==="ready"`——早期失敗＋快速重試會自動補滿餘額、長時間失敗早已超過即刻淡出，所以不需要「重試不套最短顯示」的例外。掛載後 5s 仍 running 才淡入逾時提示；`failed` 切錯誤態（訊息＋重試＋可展開「詳細資訊」，技術原文只在該折疊區露出，不進任何 user-facing 插值位）。版本號取 `getVersion()`（讀 `tauri.conf.json`，非 `/api/health`——錯誤態正是最需要版本號而 sidecar 沒起來的時候），取不到就不顯示。動畫分入場弧與持續態兩段（一次性入場撐首開的 ~14s 會像卡住、純迴圈又浪費入場那一刻）：羽毛落定＋wordmark 字距收攏＋標語浮現，持續態是羽毛 4.2s 呼吸與五顆呼吸點（語言取自既有 session 活動指示）；`prefers-reduced-motion` 全退成淡入。品牌三段字串不進 catalog、兩語同顯英文（§4.6.13 記錄例外，與 Onboarding 一致）。視覺權威 `docs/design/a-splash.html` | `Splash` |
| `components/Onboarding.tsx` | 首次設定全屏 wizard（is_first_run 觸發）：頁面容器，序列與進度條走 `lib/onboardingSteps`。**歡迎頁是二選一**（全新設定／我有備份），選擇即決定 `mode` 並前進——路線存在元件 state。全新設定＝歡迎／根目錄／環境／登入／共通設置／系統設置／完成（單帳號六頁）；移機＝歡迎／備份包／落點／專案路徑／安裝／環境／登入／修復／完成，其中 `bundle`／`targets`／`paths`／`install`／`repair` 目前是 `migShell()` 空殼（只有標題與導覽，票 02–08 逐一填實），`env`／`login`／`done` 沿用既有卡片（票 08 補移機文案）。**移機的落點頁是獨立的 `targets` 身分、不與 `roots` 共用**：兩者都是不可逆的落檔動作但走不同端點（`onboard` vs `adopt-config`），共用身分等於讓後續 gating／續作／定位都得再判一次 `mode`，漏一處就走錯提交路徑（Codex F3）。導覽 state 存 `WizardStep`，換頁走 `stepIndex()` 身分定位。以下敘述皆指全新設定的 `roots` 頁：**根目錄頁落檔後留在精靈**（ADR-0003：`onboard` first-run only，之後各頁走細粒度端點）；清單來源是 `shownRoots`＝`draftRoots` 有內容就用它（首次啟動才有 scan-preview 即時計數），否則用設定檔既有的 roots（專案數由 store 已載入的 projects 依所屬 root 算）——**重跑引導時 `draftRoots` 必然是空的**，只認它會讓根目錄頁變成「設定檔已建立」配一張空清單、且主按鈕被「這次沒新加根目錄」擋成永久停用＝精靈卡在第二頁（票 29 驗收實測）；落檔後主按鈕只是導覽，不再受 draft 數量限制。落檔狀態記在元件 state（不讀 `config.is_first_run`——後續 config write 會使該欄變 undefined，Codex F-7），落檔後根目錄清單轉唯讀、主按鈕只前進不重打 onboard；換頁清暫態訊息並捲回頂端；歡迎頁掛 `<LangSwitch>`；環境頁掛 `<EnvCard>`、登入頁掛 `<LoginCard>`、共通設置頁掛 `<CommonConfigCard>`、系統設置頁掛 `<SystemSettingsCard>`（四者都含該頁標題與導覽，系統設置頁的第三張範本卡待票 28）；依 scan-preview status 擋無效 draft、denied 用 amber notice；字串全走 `t()`（onboarding ns） | `Onboarding` |
| `components/CardTerminal.tsx` | 設置卡內嵌終端機（安裝／登入／備份共用；選填 `onEnded` 透傳給 `Terminal`）：標頭（`ChevronRight` + 標題）+ `.b4-term-mount` 固定高度容器包 `<Terminal>`。`Workspace` 之外的掛載點——tabId 由 `useCardSession` 合成，store 內無對應 tab；標題只放「實際會跑的命令」（安裝，值來自後端 payload）或卡片識別（登入），前端不自組命令字串 | `CardTerminal` |
| `components/EnvCard.tsx` | 精靈環境頁（偵測 + 一鍵安裝）：`fetchSetupStatus` 結果依 `tier` 分核心／常用兩區，每列＝狀態點＋名稱＋版本或 binary 名＋狀態 chip；未安裝且有 `install_command`→「安裝」按鈕（展開執行前確認面板：顯示後端給的完整命令＋確認／取消，**上游 spec §5 硬性要求**），僅手動安裝（`install_command` 為 null）→「複製指令」展開面板顯示後端給的完整官方指令＋`writeClipboard`（成功才顯示「已複製」，2s 後復原、unmount 清 timer）；兩種面板共用一個 `expandedId`（同時只展開一列）；確認後以 `createSession({kind:"install", installId})` 建 session 並在該列下方掛 `<CardTerminal>`；session 生命週期（一次一個、關舊再建新、卸載收 PTY）走共用的 `lib/useCardSession`，合成 tabId 在 store 內無對應 tab → `setTabStatus`／`setTabActivity` 找不到就原樣返回、不建新 `tabs` 陣列，訂閱者不重繪；確認面板已用 `env.installHint` 告知離開會中斷；終端機的顯示條件**刻意不 gate `installed`**（跑完轉已安裝正是成功結果，輸出要留在原地）；「重新檢查」重打端點，失敗顯 `errors.status_failed` 且保留舊結果；PATH 與 git 身分只給靜態提醒（spec-b4 §1 的 3e 降級，本版不偵測）；狀態不落存（spec-b4 §4）；本檔不得出現任何命令字面值 | `EnvCard` |
| `components/LoginCard.tsx` | 精靈登入頁（票 25）：每個登記帳號一張卡（顯示 key 與 `config_dir`）＋ Codex 一張（**全域登入、不做成 per-account、也不掛在帳號數上**，B-1 收尾票已確認；後端對 `login_target=codex` 免帳號驗證，前端因此不必借一個帳號去通過驗證；"Codex" 與 Fledge wordmark 同屬 §4.6.13 記錄例外，不進 catalog）；卡片以陣列描述（`{id,title,desc,account?,target}`）後統一渲染——`account` 為 undefined 即代表不綁帳號；按鈕建 `kind=login` session（`path:""`——後端固定跑在 home）並在該卡下方掛 `<CardTerminal>`，生命週期走共用的 `lib/useCardSession`（一次一個、關舊再建新、卸載收 PTY）；終端機掛著時顯示 `login.leaveHint` 警示離開會中斷（`login.note` 叫使用者看終端機輸出，不講清楚會自相矛盾）；第一張帳號卡的按鈕吃 `primary`（對齊 demo）；**刻意不顯示綠勾／完成狀態**——macOS 的 Claude 憑證在 Keychain 而非 `config_dir`，前端沒有可靠成功訊號（spec-b4 §1），改以 `login.note` 說明本頁只負責「開一個環境正確的終端機」；終端機標頭只放卡片識別、不重述命令（命令由後端 `_resolve_command` 決定）；錯誤映射 `errors.unknown_account`／`errors.login_failed`；提供「稍後再登入」直接往下一頁 | `LoginCard` |
| `components/CommonConfigCard.tsx` | 共通設置卡（票 26 精靈版／票 29 設定頁版，**同一元件兩處掛載**）：設定頁版由 `allowOverwrite` 開啟逐項授權（needs_overwrite 的列給勾選框，送進 apply 的 `overwrite` 是 `(account, entry)` pair〔ADR-0002，裸 entry 名會讓授權跨帳號外溢〕）。**授權有三條失效規則**：換 ctx 清空、**每接受一輪新 plan 就用 `pruneAuthorized` 篩掉已不需授權的項目**（跑在 `useLayoutEffect`——passive effect 要等 paint 之後才跑，那一瞬間「新 plan 已畫出、套用鍵已解除停用、舊授權還沒篩掉」是可送出的真實窗口，Codex R2 High）（衝突消失即失效——否則「衝突→被改成一致→內容又不同」的來回會讓畫面以舊授權預先打勾，使用者在沒重新看過內容的情況下授權覆蓋，Codex R1 High）、**送出即用掉**（成功失敗與連線錯誤都算；`stale` 的定義正是「寫入前發現內容又變了」，留著勾選等於沿用對舊內容的授權——清除**只作用在自己那個 ctx**，否則舊請求回來會刪掉使用者換帳號後重新勾的同名 pair，變成「勾了卻沒送出」，Codex R2 Medium）；送出／重測途中勾選框停用。送出前另以最新 `ops` 過濾一次。逐項結果 chip 與勾選框**並存**——只留結果 chip 會讓 `stale`／`failed` 那列再也回不到可授權狀態，重試路徑死掉＋卡頭「重新檢查」（設定頁沒有換頁動作可觸發重測；**不適用態的卡也有**，否則使用者補建帳號目錄後只能關掉設定頁再開）＋`st.backupHint` 明示備份檔名樣式；`onPrev`／`onNext` 皆給才渲染精靈導覽列，否則套用鍵改掛卡片下方（`b4-card-foot`）；`onPendingChange` 回報未就緒項目數給設定頁摺疊標題。精靈版行為不變：source＝第一個登記帳號（實體檔持有者，精靈不讓使用者改選）、target＝其餘帳號中 `checkDir` 回 `dir` 者——**目錄未確認存在的一律排除**（`apply` 會 `mkdir` target dir，照送等於替單帳號使用者建出他沒要的目錄），全部排除或只登記一個帳號時整張卡「不適用」灰態（`.b4-card.is-na`），文案分三種：`missing`＝「不存在，你只用一個帳號」／`denied`·`not_dir`·探測失敗＝「無法確認」（`naDescBlocked`，說成不存在是假話）／無第二帳號＝`naDescSingle`；逐項顯示現況（`STATE_TEXT` 窮舉後端 11 個 state）與將執行的動作（`chipFor`：`needs_overwrite` 先攔成「保留不動」琥珀、`skip` 但非 `ok` 顯示「無法處理」而不是「已就緒」），多 target 逐帳號分組；**apply 一律送空 `overwrite`**（spec-b4 定案 8，逐項授權在票 29 的設定頁版）、有 `needs_overwrite` 時顯示指向設定頁的琥珀說明、進階項 `projects` 只給一行說明；套用後 chip 換成逐項結果（`results.*`）並**重新 plan**（狀態即時偵測，spec-b4 §4）；主按鈕依**現況**而非「按過了沒」——`canApply`＝有非 skip 且非 `needs_overwrite` 的項目（conflict 不算，否則按了永遠回 conflict；有項目 `failed` 時按鈕自然留著可重試），否則讓位給「下一步」，`cc.nothingToDo` 只在**每項都 `ok`** 時才說（`source_missing` 也是 skip，說「已經是你要的狀態」會騙人）；判別碼映射走 `i18n.exists('errors.<code>')`（catalog 是單一真值來源，不另存一份碼清單），未映射者退 `errors.common_config_failed`（判別碼與逐項 `failed` 的 error 只進 console）；偵測結果存成單一快照 `detected{targets,plan,blocked}`（三者必須原子，否則 plan 失敗時會留下「錯誤訊息配一張說不適用的卡」）；失效機制**兩套刻意分開**：`reqId` 管 load-vs-load 的先後（同 EnvCard），`ctx`＝`JSON.stringify([port, source, accountsSig])` 管「資料上下文還是不是同一個」——apply 送出前記下 `ctx`、回來與 `ctxRef` 比對（換 sidecar／換帳號就丟棄），逐項結果也連同 `ctx` 一起存、且在 `ctx` 轉換時**連同 `detected` 一起真的清掉**（只比對相等的話 A→B→A 會讓舊 outcome 復活；不清 `detected` 的話，`port` 過渡成 `null` 時 `load()` 一開頭就 return、那張舊卡會無限期留著）。部分 target 被排除時在卡內明列（`cc.excludedHint`）——只列可用帳號會讓使用者以為全部都同步了。**apply 不推進 `reqId`**（會作廢在途的合法 load，其 `loading` 就沒人解除）；**results 不由 load 清除**（`load` 的 deps 含 `t`，切語言也會重建它）；apply 寫回前 `mounted` 與 `ctxRef` 都要過（`ctxRef` 不因 unmount 改變，只看它會在卸載後又發一次探測請求）。`ctxRef` 在 `useLayoutEffect` 更新而非 render body（render 可能被 concurrent 丟棄）。簽章一律走 JSON 而非手寫分隔符（macOS 路徑允許 `\|` 與 `=`）。effect dep 用 `accountsSig`（父層每 render 換 accounts 引用） | `CommonConfigCard` |
| `components/SystemSettingsCard.tsx` | 精靈系統設置頁（票 27）：訂閱服務清單（名稱＋月費，可增刪列）與知識庫根目錄（輸入框＋picker）兩區，第三區掛 `<TemplateCard>`（票 28）。**兩區刻意沒有各自的儲存鈕**（視覺權威 demo 的定案）：頁尾只有「全部略過」與「下一步」，於是「下一步」＝送出使用者真的**改過**的區塊、「全部略過」＝什麼都不送就前進；兩區都沒填自然等於整段略過。沒改就不送（`sameSubscriptions` 比訂閱、字串比 `kms_root`）——用一次可能失敗的請求換零變更只會擋住想直接往下走的人，且「既有值被刪光」仍會送出空清單而非靜默吞掉；空白列走 `dropBlankRows`（只填一欄的仍由 `validateSubscriptions` 擋成欄位錯誤）。**dirty 的比較基準是 `savedSubs`／`savedKms` 兩個 ref＝本卡已知、已成功持久化的 baseline（存成功才往前推），不是當下 props**——sidecar 重啟／外部改動會換掉 props，拿它跟沒被碰過的表單比會判成 dirty 而把畫面上的舊值寫回去蓋掉較新設定（Codex R1 #2）；存進去成功即更新基準，於是部分成功後的重試不重送已成功那段（也不必依賴 store 回吐 PUT 回應）。失敗訊息三分：`errors.subs_failed`／`errors.kms_failed`／`errors.kms_failed_subs_saved`（訂閱已存進去就明說，不謊稱它失敗），**原始訊息只進 console**——`configWrite` 抽的 FastAPI `detail` 是後端中文 prose，插進畫面等於讓英文使用者看到中文，也違反 spec-b4 §5（Codex R1 High）；`port` 為 null 時**有待送出的改動就擋下**（`errors.backend_unavailable`）——默默前進會讓剛填的東西憑空消失，沒改動則照樣可以往下走；KMS 存完回讀 config（比照 `Settings`），回讀失敗只 warn 不當成儲存失敗。已知限制：props 在 mount 後變動時**畫面不跟著更新**（只保證不寫回舊值），外部改 config 又同時開著精靈屬角落情境 | `SystemSettingsCard` |
| `components/TemplateCard.tsx` | 精靈系統設置頁的第三張卡（票 28）：列 allowlist 全部範本（`available=false` **照列**標「未內建」且不給部署鈕——public build 只內建 `project-starter`，藏起來會讓使用者以為版本少了東西；按了也只會拿到 `template_unavailable`）＋目的地下拉（已掃到的專案，資料**直接讀 store 的 `projects`** 不再打一次 `GET /api/projects`）＋「其他位置…」走 picker（取消什麼都不動，select 綁 `dest` 會自己彈回）。**逐列一個部署鈕**而非 demo 那顆放在「部署到」列尾的單一按鈕——清單三列時單一按鈕指涉不明，demo 是靜態示意沒模擬選擇語意（比照 EnvCard 逐列「安裝」）；部署後在該列下方列**逐檔結果**（`sys.result.*`）並**重新 plan**（狀態即時偵測，spec-b4 §4）。名稱與說明都以 **id 對 catalog**（`sys.tpl.<id>.name`／`.desc`），catalog 沒有該 id 才退後端英文 `label`／`description`——後端一律回英文（§4.6.13），拿它當常態文案會讓中文版出現英文範本名（Codex R1 #1）；使用者選過的目的地**一律留在下拉裡**（含後來從 `projects` 消失的專案），否則 select 顯示空白、部署卻寫往看不見的舊路徑（R1 #2）；預覽走 `Promise.allSettled` 逐項隔離，一個範本壞掉不讓其餘成功的 chip 一起消失（R1 #3）；狀態 chip 走 `STATE_CHIP` 窮舉後端四個 `TemplateState`（後端加狀態即編譯失敗），逐檔結果走 `OUTCOME_CHIP` 表且**查不到退 `sys.result.unknown`**（動態組 key 會把 i18n key 原文顯示給使用者，R1 #5）；判別碼映射**刻意用顯式表 `ERROR_TEXT` 而非 `errors.${code}` 動態查**——`probe_failed` 在共通設置卡是「讀不到帳號設定目錄」、在這裡是「讀不到部署目的地」，同碼兩文案；表外的碼退 `errors.template_failed`、原碼只進 console。`plans`／`results` 都綁 `ctx`（`[port, dest]`）失效，`busy` 一律解除（票 25 R4） | `TemplateCard` |
| `components/DevEnvSection.tsx` | 設定頁「開發環境」摺疊區（票 29）：嵌 `<CommonConfigCard allowOverwrite>` 與 `<TemplateCard>` 兩張常用卡＋`st.rerunHint` 內的 `<rerun>` 具名標籤映射成「重跑引導」按鈕（modal 狀態在 App，這裡只往上通知；App 先關設定頁再開精靈，兩個 modal 不疊）。**卡片即使收合也照樣掛著**——待處理數要靠共通設置卡偵測才知道，等展開才數等於數字永遠遲到一步；代價是開設定頁時多幾次唯讀的本機探測。`handlePending` 走 `useCallback` 穩定引用（卡片以它當 effect 依賴） | `DevEnvSection` |
| `components/BackupCard.tsx` + `BackupCard.css` | 設定頁備份卡（票 02）：未設定時顯示引導與「選擇備份位置」，已設定時顯示天數主行（分級只用文字顏色，不加徽章也不加 banner）＋路徑與「更改」＋既有備份包清單（預設 5 筆可展開，**之後由還原精靈複用**）；「預覽會收什麼」／「立即備份」兩顆按鈕（共用一個卡內終端機、一次只跑一個；**忙碌判定是 `starting || (running && !finished)` 而非「有沒有 session」**——跑完的終端機刻意留在原地讓人看輸出，用後者會讓按鈕永遠鎖著；**忙碌時連「更改位置」也停用**——session 建立時後端已把當時的 backup_dir 固定進 argv，中途改位置會讓備份寫舊處、回讀掃新處，終端機顯示成功但卡片找不到那份備份包）；執行中顯示「關閉設定會中斷」提示（**設定頁是條件掛載的 modal，卸載會經 `useCardSession` cleanup 終止子程序**——把 job 生命週期搬出元件與這張卡的範圍不相稱，改用 EnvCard／LoginCard 對同一情境既有的明講做法）、輸出走 `<CardTerminal>`（生命週期用共用的 `useCardSession`；**回讀判的是 session 的「有→無」轉換**而非「現在是 null」，後者掛載時就成立會讓每次開卡片多打一次請求）；九種阻斷原因（未設定／containment 四態／目錄三態／缺腳本／缺 python3）各自可分辨，同時成立時取優先序最前的，且與後端擋下的順序一致；`last_attempt_failed` 是**正交的修飾而非狀態**，多一行提示但不停用任何操作——使用者要做的正是再按一次備份。**刻意只是常駐可見指標**——不自動備份、不排程、不提醒、主畫面零新增元素（設計決策）。`latest-request-wins` 的 `reqId` 比照 EnvCard（sidecar 重啟換 port 會讓新舊請求重疊）；判別碼經 i18n 映射、未知碼退通用訊息，例外原文只進 console；讀不到狀態整張卡不渲染（沒有可採取的行動，空殼只是噪音）；容器沿用 Onboarding.css 的 `b4-card` 家族，`.css` 只放備份專屬版面 | `BackupCard` |
| `components/RestoreCard.tsx` + `RestoreCard.css` | 設定頁還原卡（票 09，接在備份卡下面）：選備份包（radio，複用 `status.bundles` 與 `formatBundleTime`／`formatSize`）→ 展開位置（後端算的預設值＋系統選擇器可改，每次改都重新 `restorePlan` 驗證）→ 執行（`kind=restore` PTY，展開與差異報告都在 `<CardTerminal>` 裡）。共通設置斷鏈檢查是**獨立的一區、卡片掛載就掃**（Codex 審查抓到：還原只解到獨立的 DEST，展開本身不可能讓現役目錄冒出斷鏈——斷鏈出現的時刻是使用者把設定**搬回**現役目錄之後，綁在展開後只會讓它幾乎永遠說「沒有斷鏈」；順帶化解「PTY EOF 不帶結束碼、展開失敗也會顯示修復鍵」）；文案明講檢查的是現役目錄、與剛展開的備份包無關。**修復不自動執行**（改寫 symlink 是破壞性操作，要明確按下去）；斷鏈偵測直接用既有的 `commonConfigPlan` 數 `broken_link`，不另做端點；修復送 `RESTORE_REPAIR_ENTRIES`（含 `projects`）、逐項結果的 chip 與色調沿用 `CommonConfigCard` 匯出的 `OUTCOME_TONE`（同一個 outcome 在兩張卡是同一件事），`skipped` 不列（一次修復大多數項目都沒事做，全列會淹沒真正動到的那幾筆）；忙碌判定與「關閉設定會中斷」提示比照備份卡；判別碼經顯式表映射、例外原文只進 console | `RestoreCard` |
| `components/AccountsEditor.tsx` | 設定頁帳號編輯區：加/改 config_dir/改 label/刪（級聯轉移面板）+ config_dir 警告依 DirStatus（不存在/不可讀/非資料夾） | `AccountsEditor` |
| `components/LangSwitch.tsx` | 語言切換膠囊（中文／English autonym 不進 catalog）：`await i18n.changeLanguage` 成功**才**寫 localStorage `fledge-lang`（i18n 權威序最高階），兩步各自 try/catch——切換失敗不動快取、Storage 失敗只是記不住不擋切換；`aria-pressed` 標目前語言。定位交給呼叫端容器，設定頁 header 與精靈歡迎頁（`.ob-lang` 絕對定位右上）已掛 | `LangSwitch` |
| `components/Logo.tsx` | 品牌三色填色羽毛標（去背 PNG，與桌面 app icon 同源）共用元件；`<img>` 引用 `assets/fledge-feather.png`，接受 `size` prop（＝高度 px） | `FeatherMark` |
| `components/Workspace.tsx` | TabBar+Terminal 合成一體面板（保留全 tab mount + display 切換）；**不再收 `connError` prop**——致命的啟動失敗已由 `Splash` 的錯誤態承擔，而該分支 gate 在 `port == null`、`loadProjects` 失敗時 port 早已設好＝那段訊息永遠渲染不到；`port == null` 只留「連線中…」（啟動期間被 Splash 蓋住，實際只會在 sidecar 中途掛掉的空窗出現）；tab 狀態矩陣與 ended/offline 子狀態；kind 分派加 `memory`→`<Memory>` 分支；Terminal 分支（claude/terminal + projectPath）內掛 `<RelatedFloat>`（既有 absolute wrapper 為定位包含塊，不改終端機尺寸）、並把 `projectPath` 傳進 `<Terminal>`（Cmd+click 連結的相對路徑解析）；`ws-term-area` 設 `useDroppable`（id `terminal-drop`，需求 4 drop target） | `Workspace` |
| `components/Memory.tsx` | 記憶面板容器（**master-detail 雙欄**，純前端單例分頁）：持有 selection（複合 `{path,groupKey}`／`{projectKey}`）+ facet + group 展開 Set + body 快取（key=`path@mtime`）；30s 輪詢 gating（active+!hidden）；**q-guard**（q 非空時不清 selection、清搜尋後右欄復原 pin）；組 `<MemoryIndex>`（左）+ `<MemoryDetail>`（右）| `Memory` |
| `components/MemoryIndex.tsx` | 左欄：標題+scan_meta（掃描/搜尋總數）、搜尋框（後端 q）、facet chips（前端在地過濾 source/type）、可收合 groups（`applyFacet` 後渲染、計數=可見數）| `MemoryIndex` |
| `components/MemoryGroup.tsx` | 可收合 group：caret 獨立按鈕（stopPropagation 只收合、aria-expanded）+ 選取區（專案→選專案）+ 計數 + 建議 badge；專案展開預設、其餘收合 | `MemoryGroup` |
| `components/MemoryRow.tsx` | 單行密集列（badge + 標題 + 截斷摘要 + 已連結 ◈）；`<button>` + aria-selected | `MemoryRow` |
| `components/MemoryDetail.tsx` | 右欄三態：空狀態 / item 全文（badge+meta+`<pre>` body）/ 專案脈絡（相關 chips + `SuggestionChip`）| `MemoryDetail` |
| `components/SuggestionChip.tsx` | 建議 chip（topic + ✓✕，aria-label 走 `t()`）：await 真成功（wrapper !ok 會 throw）才 `onAfterWrite`、失敗顯 error 不樂觀消失、pending 雙路徑 reset | `SuggestionChip` |
| `components/RelatedFloat.tsx` | 終端機右下懸浮（claude/terminal 分頁）：`fetchMemoryRelated` 該專案相關連結+建議；收合 pill ↔ 展開 popover；count 0 回 `null`（無連結即隱形）；`position:absolute` 不奪終端機尺寸/scroll | `RelatedFloat` |
| `styles/term-theme.ts` | 從 CSS 變數讀終端機色組，回傳 xterm `ITheme`；讀取時機：xterm 初始化 + 主題切換 | `readTermTheme` |
| `index.css` | CSS 變數 token 層（`:root` 字型 + 終端機 ANSI 16 色〔深底兩主題共用〕、`[data-theme]` 語義/表面/終端機 token）；各 UI 元件 CSS 均從此繼承，末尾含 `prefers-reduced-motion` 全局重置 | — |

> 各 UI 元件均有同名 `*.css` 兄弟檔（如 `Sidebar.tsx` ↔ `Sidebar.css`），品牌 token 均從 `index.css` 繼承，不重複定義色彩值。

### sidecar/tests/ — sidecar 測試（鏡像對應）
| 檔案 | 測什麼 |
|------|------|
| `test_health.py` | health endpoint |
| `test_app_config.py` | 設定讀寫 round-trip + 寫入 helpers + 原子寫＋票 07（`create_if_absent` 只建一次、已存在拒且逐位元組不變；`extra` 欄位 round-trip、缺欄位／非 dict 容錯為空） |
| `test_project_scanner.py` | depth=1 掃描、排除隱藏、root 欄位、套 override |
| `test_projects.py` | /api/projects；tree endpoint（containment/kms_root deny/400/403/denied|missing|not_dir 200） |
| `test_dir_tree.py` | `list_dir_entries`（dotfiles 排除/資料夾在前+名稱排序/空目錄/path 欄位） |
| `test_paths.py` | containment helper（等於 root/子路徑/prefix 偽命中/root 外/trailing sep）＋目錄同一性 helper（APFS 大小寫別名、尚不存在的目錄不拋例外）＋既有 path 正規化 |
| `test_config_routes.py` | config 寫入 endpoints（鎖/驗證/防呆/持久化） |
| `test_pty_bridge.py` | PTY round-trip、env override、env_remove 剔除、並發、`close()` 失敗時的 handle 去留（仍活著留、已死或問不出狀態丟）與 `on_close` 時序（被放回時不收尾 usage span、重試成功才收且只收一次） |
| `test_sessions.py` | session 建立 + WS echo + resize + 模式 A + flow control（`_apply_flow_control` 單測 + pause 真閘住 PTY→WS/roundtrip/text 不入 PTY/壞 frame 不斷線/paused EOF→4001）+ kind=install/login（allowlist 命令/未知 install_id 400/不注入帳號 env/login 注入帳號 env/codex login/未知帳號 400/未知欄位 422） |
| `test_usage_pricing.py` | 定價正規化（別名/後綴/synthetic/表外未知款不猜價）＋兩源計價公式 |
| `test_sync_pricing.py` | `admin/sync_pricing.py` 的表建構與渲染（假上游 dict、不連外也不掃本機）：裸 key 過濾／日期版合併／同名不同價視為衝突／PINNED 保值／EXCLUDED 不入表／cached 缺失退回 input 價／渲染結果可 exec 回原表／版本隨內容變 |
| `test_usage_parser.py` | 兩格式解析（容錯/cache precedence/差分 clamp/壞 ts/中文 cwd/rate_limits 末筆） |
| `test_usage_scanner.py` | realpath 去重（symlink 帳號）＋codex canonical 三層 tier＋fallback |
| `test_codex_usage.py` | Codex 實時額度（normalize_usage 欄位映射/缺鍵→bad_response、fetch no_auth/401→unauthorized/URLError→network/壞 JSON；opener 注入不打真網路、token 進 header） |
| `test_usage_cache.py` | L1 命中/L2 roundtrip/損壞重建/pricing 重算/generation 防舊蓋新/並發序列化/ghost |
| `test_usage_aggregator.py` | dedup 替換規則/KPI 視窗/分源命中率/horizon/synthetic 濾除/壞訂閱防衛 |
| `test_usage_routes.py` | dashboard route（冷掃 202→ok/days 收斂/last-good/穩態 ok/error 語義/訂閱 PUT 驗證） |
| `test_usage_perf.py` | 效能煙囪（slow marker）：200k 條冷掃/增量/單檔變動/build_dashboard 計時 |
| `test_memory_parser.py` | frontmatter/wikilink（含 alias 取 target）/title fallback/malformed no-raise/missing→None |
| `test_memory_scanner.py` | native 三態（matched/orphan/ambiguous 碰撞）/跳 MEMORY.md/KMS containment + 逃逸 symlink 擋下/None root |
| `test_memory_links.py` | 無向去重+持久化/0600/dismiss 綁 pair/suggest 名稱包含+短名只手動/`_norm`/20 執行緒並寫不失 |
| `test_memory_aggregator.py` | scope mapping/global·projects·kb 分組/orphan·ambiguous 可見/已連 topic 掛 project/overview 不含 body/body 命中回 snippet |
| `test_memory_config.py` | kms_root round-trip（raw 含 ~）/清空 |
| `test_memory_routes.py` | overview shape/links CRUD+dismiss/item 擋 traversal（403） |
| `test_install_specs.py` | 資料表覆蓋 core 工具/欄位齊全/`get_spec`/`get_install_command` allowlist（未知 id、僅手動→None） |
| `test_install.py` | 移機 `backup/install.py`（全程假 HOME＋tmp_path）：`plan` 唯讀（計數／既有標 skip／`.claude.json` 排除／無 manifest 拒絕／config_dir 是 home 或祖先拒絕）＋`install`（空落點裝齊／既有逐位元組不變標 skipped／重跑冪等零變化／plan→install 間來源被抽換整批停手／target 被抽換該帳號停手且替身零寫入／account root 的 fsync 以 inode spy 釘住／file symlink 與斷鏈的 plan-install 數字對帳／FIFO 不 open 不數／指向外面的目錄 symlink 不跟隨／`.claude.json` 不落地且列 excluded／symlink 六態〔同帳號連結解得開、範圍外絕對路徑／相對逃逸／指帳號根或祖先／循環／範圍內型別符但非本次發布 皆 excluded〕／journal 完整成功清除、失敗保留、壞行容錯／transaction_id 換 staging 或改落點即變／journal 讀不出降級判 failed 保留／第二階段 fd-relative（階段間換中間目錄不跟隨、dir_fd spy）／journal 開啟不跟隨 symlink（journal／.fledge 兩態，目標零污染）／provenance 讀取走 pinned fd（開啟後換 journal symlink 指偽 journal，injected node 不授權，mutation 驗過紅）／目錄記 journal・provenance 判準・fd-relative・dir_fd 四項各 mutation 驗證過紅／extra 帳號外資產搬到使用者確認落點且帳號連結解得開（plan 亦數 extra；授權集合・plan 掃描・install 安裝三處與未確認 excluded・name 重驗・落點驗證共六 mutation 驗證過紅）・沒確認落點整項 excluded 零落地・extra name path-like 拒・extra 落點驗證同帳號（home／祖先拒）・manifest.extra 非 {str:str} 拒・extra 來源開失敗記 failed 保留 journal（extra/ 與項目兩態、不存在≠開失敗；此三條先紅後綠）・extra 來源綁 plan 時身分（換真目錄／刪單項／刪 extra/／plan 後才出現皆 `source_moved` failed 保留 journal，四條先紅後綠；備份包從頭就沒有的兩個 ENOENT 入口維持靜默且完整成功清 journal，兩分支各 mutation 驗證過紅）・帳號側來源身分比照 extra（換真目錄／刪帳號內容／symlink 換走／plan 後才出現皆 failed，四條先紅後綠；plan 時就沒有的帳號 ENOENT 靜默、其餘帳號照裝，共用 helper 靜默分支 mutation 驗紅）・落點重疊（plan 字串重驗兩態〔帳號巢狀・帳號×extra〕先紅後綠；install 寫入前 fd 版——plan 後中間目錄換 symlink 收斂成同一目錄／互為祖先，兩落點全 failed 零內容落地，mutation 驗紅）・專案改名〔票 06〕（mapping 改名且歷史檔逐位元組同包內／memory 子目錄跟著搬／staging 整棵位元組不變／未對應照搬＋列 unmapped_projects／帶 mapping 重跑冪等 will_install=0／碰撞三態・非絕對・old 不存在拒且零寫入／改 mapping 換 transaction_id／project_paths 讀 cwd 給建議值——七項 mutation 驗證過紅／R1 三修〔jsonl symlink 不跟隨不外洩・讀取上限・FIFO 三層不阻塞（修復前行為是卡死無法驗紅，由測試能跑完承擔）・old 相對字串拒・有損別名 cwd 不一致 ambiguous・symlink 指進改名專案跟著改名並回寫新位置，除 FIFO 外皆先紅後綠〕／票 09（祖先換 symlink／換真目錄拒且零外寫・中間元件後建容忍〔mutation 驗紅〕・node 換同名新 inode／換 symlink 皆 failed node_identity_mismatch 且 journal 保留・同輪與跨輪 reparenting 雙方停寫零混裝，六條先紅後綠；收斂測試改 APFS 大小寫別名構型〔symlink 構型被祖先釘鎖提早攔下〕）〔票 05〜09＋Codex 各輪〕） |
| `test_env_detect.py` | 偵測純函式（裝了探版本/沒裝跳過/探測失敗與 timeout 容忍/`detect_all` 一 spec 一 status） |
| `test_setup_routes.py` | `/api/setup/status`（monkeypatch detect_all 注入假資料、回傳 shape）＋共通設置兩端點（plan 預覽不動 FS／apply 建連結／overwrite 閘與 pair 授權隔離／`setup_lock` 並發序列化／400 判別碼／未知欄位與 client 夾帶 plan → 422／config 壞掉與探測 `OSError` 回判別碼不裸 500）＋範本三端點（available 旗標／預覽不動 FS／deploy 真寫檔／四個 400 判別碼與 422／並發 deploy 序列化／`OSError`→`probe_failed`） |
| `test_safe_fs.py` | 複製原語（短寫寫滿／`O_EXCL` 不覆蓋不寫穿 symlink／零長度寫視為失敗／失敗清半截檔／`dir_fd` 相對寫入）＋原子發布原語（內容完整才以最終名出現且暫存不殘留／既有目標拒絕且逐位元組不變／fsync 先於 link 且同步對象 inode 釘死〔「刪 fsync」「fsync 錯 fd」雙 mutation 驗證過紅〕／link 失敗清暫存／寫入失敗也清暫存）＋errno→判別碼映射（含 `ENOTDIR`、`ENOTSUP`／`EOPNOTSUPP` 與 fallback） |
| `test_templates.py` | 範本資料層與部署：allowlist／manifest 產生與載入的全部拒絕規則（含 `..` 鏈祖先全宣告的繞道）／父在子前正規化／目的地防呆／`probe_entry` 三態與逃逸父目錄／`plan` 四態與 conflict 停 subtree（含同前綴兄弟不誤擋）／`deploy`（永不覆蓋、只建最後一層、深度巢狀、兩種 fd 競態、祖先掉包→`destination_moved`、`stale`、穩定判別碼、稽核 log）／repo public seed 的 manifest drift |
| `test_template_manifest.py` | artifact manifest：分類取自 spec 表／雙向對帳／雜湊重算／staged 根白名單／`included=false` 的 schema 與空目錄／缺席 id／symlink（含 manifest 也登記它的情形）／`stage_templates`（public-mode 排除 private、來源含 symlink 或 symlink 目錄即失敗、未知 id、分類放錯來源） |
| `test_release_workflow.py` | release workflow 結構鎖（以 PyYAML 解析並剝註解行）：上傳步驟必須用 literal `|`（folded 會讓一個 `#` 吞掉整串）／驗證器與 drift 檢查必須在同一步驟且順序在上傳之前／private staging 前提斷言存在／release 不得帶 `--with-private-templates` |
| `test_common_config.py` | allowlist／graph 防呆（target≡source、雙向祖先—子孫 overlap、target 彼此巢狀、home 祖先與根、平行 sibling 不誤擋）／十一態探測（含 source 型別前置、逃逸連結不判 ok）／`_action_for` 涵蓋全部十一態／apply 各分支／備份不覆蓋既有備份／TOCTOU stale／target dir 掉包與每 op 重驗／relink 不誤刪實體檔且不覆蓋競態新檔／per-account 授權隔離／symlink 不寫穿且原連結入備份／短寫寫滿；`repair`（假 HOME＋tmp 全隔離，連「舊機器路徑」都造在 tmp 內刻意不建立）：斷鏈重指本機並讀得到內容／已正確的連結與使用者刻意的 alternate link（wrong_link）inode 皆不變／清單外斷鏈原封不動／source dir 不在·被檔案占住·被換成 symlink·讀不到皆 raise 判別碼且不動任何連結／混合狀態逐項回報且非修復項資料完好／單項失敗不影響其餘／TOCTOU stale／op 不屬 graph |
| `test_restore.py` | 還原決策層：備份包名 allowlist（路徑穿越／絕對路徑／存在但非備份包形狀／時間戳無效全擋）／預設展開位置由備份包時間戳決定／展開位置防呆（來源樹重疊·home·根·非空·檔案占位·讀不到，且 containment 排在空不空之前）／argv 不經 shell |
| `test_restore_routes.py` | `POST /api/restore/plan`（預設位置／驗證選定位置／位置問題回 200 旗標而非錯誤／未知備份包 400／相對路徑 400／備份目錄三態／未知欄位 422）＋`kind=restore`（spawn 前重驗一切、閘順序、缺腳本與缺 python3、後端組 argv 且備份包名在後端才解析成路徑、未給位置用預設）。成功路徑攔截 `create_session`，不真的展開。移機端點（票 03）：install-plan 從 config 讀落點／非 bundle 400／install 重算 plan 並真寫檔／client 塞 plan → 422 零寫入／預覽零寫入且 `will_install` 與實際 installed 數一致／config 未落檔 → `config_not_initialized` 400（假 HOME＋不撞 DEFAULT key 的雙層保險測法）／config 缺 accounts 欄位 → `config_unreadable` 500 永不用 DEFAULT 帳號／損壞 bundle（accounts 非 mapping、實體 accounts/ 缺失）→ `source_not_a_bundle` 400 非裸 500。adopt-config（票 07）：沒帶 accounts → 422 零落檔／落點重驗（home 拒・帳號×extra 互為祖先拒）／已存在 409 逐位元組不變／非 manifest 宣稱的 key・name 與同名重複拒／root 指向未確認帳號拒／成功寫 raw 且 manifest 舊機路徑零落地／**F2 端到端**：確認 extra → install-plan 數進預覽 → install 落地且帳號連結解得開／config 無 extra 確認 → 經 API 列 excluded／config 手編成巢狀落點 → install-plan 400 `overlapping_config_dirs` 非裸 500。票 06：project-paths 唯讀列專案（cwd／建議值）＋非 bundle 400／mapping 經 install-plan（改名表＋未對應清單）與 install（歷史落到新名下）一路生效、mapping 錯誤 400 判別碼 |
| `test_restore_shell.py` | `restore-claude.sh` 行為契約（假 HOME＋自造備份包，用**真的備份腳本**產包以同時鎖住兩支的相容性）：備份包來源必填／無個人路徑／解壓失敗時 DEST 不存在且無 staging 殘骸（假 tar **先寫再失敗**，直接 exit 1 的假 tar 測不出原子性）／缺 manifest 明確拒絕且不留半套／非空 DEST 拒絕且原內容不動／空的既有 DEST 可用／三類差異各自出現／**現役目錄內容快照前後相同**／`--diff-only` 不重解／**惡意包**（票 12）：symlink manifest 拒絕且**哨兵字串不出現在輸出裡**〔擋下來卻仍印出內容等於沒擋〕、`--diff-only` 同樣拒絕、**manifest 指定的任意路徑不被當現役側掃描**〔兩條：key 對不上、以及**key 對得上時 path 仍不參與**——只有前者的話攻擊者猜到 key 就能繞過；攻擊要成立需包內有同名頂層目錄以通過「只在現役有」的過濾，測試一併造出來，否則測到的是過濾條件而非信任邊界〕、**惡意 account key 因名字不合法被拒**〔斷言的是「名稱不合法」而非「查不到」——後者是偶然；整組形狀都釘住：`/abs`／`../outside`／`a/b`／`.`／`..`／空字串／含換行〕、**畸形 manifest 形狀在發布前被拒**〔accounts 是 list／頂層是 list／extra value 非字串；斷言不留 DEST 且無 traceback〕、**移機到新 HOME 仍產出有內容的差異報告**〔守住 R1 修法踩到的假空報告；與安全那兩條互為對向，改回任一個錯誤方向都會有測試變紅〕、tar bomb 依壓縮比拒絕且 staging 清乾淨、**誠實的大包不被誤擋**〔不可壓縮內容比例≈1〕、**大量零大小成員依項目數拒絕**、**hardlink 不重複計數**〔數字實測挑過：先寫的「3 MiB×4 條」版本兩種實作都放行＝假綠〕、三種逃逸成員**各自獨立一條測試**〔混在同一個包且忽略 return code 的版本，腳本整個壞掉也會綠——Codex 抓到的假綠；現在各自斷言 tar 的機制訊息（`Path contains`／`through symlink`），措辭變了就代表依賴的行為可能也變了〕 |

### build / 環境
| 檔案 | 用途 |
|------|------|
| `sidecar/build_binary.sh` | PyInstaller 打包 sidecar 成 onedir 資料夾 + nested ad-hoc 簽章，rsync 到 `binaries/`；另含範本 staging 階段（public 預設／`--with-private-templates` 才吃 `FLEDGE_PRIVATE_TEMPLATES_DIR`、逐範本重產 manifest、產 artifact manifest、打包前先跑驗證器）＋`--add-data` 收 `build/templates` 與 `scripts/` 三個檔（備份腳本、共用來源清單、還原腳本；漏收任一個只在打包版現形，`test_backup_script.py` 有結構鎖）。判定全在 `setup/template_manifest`，shell 只做 flag 解析與串接 |
| `sidecar/resources/templates-public/` | 可進 repo 的 public 範本種子（`project-starter`：CLAUDE.md + docs/README.md + 提交版 `manifest.json`）。內容屬產品決策，改寫不需動程式碼；改動必須連同 manifest 一起提交（drift 檢查會擋） |
| `scripts/verify_templates_artifact.py` | 薄 CLI 包住 `template_manifest.verify_artifact`（`--staged-root`／`--manifest`／`--allow-private`），供 `build_binary.sh` 與 release workflow 呼叫；違規或讀不到 manifest 一律 exit 1 |
| `scripts/backup-claude.sh` | 備份 Claude 生態（`config.json` 內**所有帳號**的 `config_dir` + `EXTRA_PATHS`）成帶時間戳的 tar.gz。範圍依「可再生性」分層（ADR-0004）：`ASSET_DIRS`／`ASSET_FILES` 收，`KNOWN_SKIP` 明列不收——後者存在的目的是**偵測清單外的新項目並出聲**（Claude Code 新增目錄時要有人重新判定）。另一條完整性防線：`skills/` 內指向備份範圍外的 symlink 會警告「連結會存、內容不會」（實際踩過 `~/.claude/skills/huashu-design → ~/.agents`，故 `EXTRA_PATHS` 收 `~/.agents`）。**永不收憑證**、**來源全程唯讀**、**不自動刪任何備份**；staging 走 APFS clone（同卷零成本）、打包後 `tar tzf` 驗證失敗即刪半成品；輸出檔已存在則拒絕覆蓋 | — |
| `scripts/restore-claude.sh` | 展開備份包並列出與現役的差異（只在備份／只在現役／兩邊不同三類），**不寫任何現役目錄**（ADR-0004）——搬什麼回去由使用者決定。展開目標非空即拒絕；**containment 在腳本自己這一層也有一份**（根目錄／家目錄／落在任一現役來源裡面都拒絕）——判定整段收在**一個 Python 區塊**裡、不在 bash 與 python 之間傳路徑清單（含換行的路徑會被 `read` 拆成兩個 root＝資料與分隔符混用），且來源解析與比對邏輯與 `containment.py::source_roots`／`paths.same_dir`／`paths.is_same_or_within` **逐條對齊**（畸形 account 退回 `~/.claude` 而非略過；`rstrip(os.sep)` 讓 root 為 `/` 時不會組出 `//`；大小寫別名走 inode 身分上溯而非無條件轉小寫〔case-sensitive volume 上折疊會過嚴〕；比對用字面路徑不用 realpath〔dest 是指向外面的 symlink 時 realpath 會放行，但 staging 建在 `dirname(DEST)`、仍在來源樹內〕）；設定檔**不存在**才退回 `~/.claude`（移機到新機還沒設定 Fledge 是最常見的還原情境），存在卻讀不懂（損壞／無權限／是目錄或壞連結）一律拒絕——fail-open 最容易藏在 fallback 裡；**發布走 `os.rename` 而非 `mv`**（`mv A B` 在 B 是既有目錄時會把 A 移**進去**，於是另一個程序在解壓期間搶先發布時，我們會把整棵樹藏進它裡面、還因為它的 manifest 在而照樣印出成功報告；rename(2) 的語意正是「目標不存在或是空目錄才成功」且原子）；**中斷後不留殘骸**：EXIT trap 在掛斷時會跑，但關掉設定頁時 ptyprocess 0.3 秒後就送 SIGKILL、而 `rm -rf` 幾百 MB 要好幾秒（真機驗收因此在家目錄留下 525MB 隱藏目錄），故下一次還原**回收超齡殘骸**〔嚴格 regex 覆核 basename；**判準以名字裡的 PID 為主**——產生它的程序已不在＝確定棄置，立刻回收，因為「中斷→立刻重跑」是最自然的反應而只靠年齡閘構不到它；PID 還活著才退回 60 分鐘年齡閘（PID 被回收給不相干程序時用），比 backup 的 24 小時短是因為這邊是家目錄裡最大幾百 MB 的目錄〕；**先解到 staging、驗過 manifest 才整棵改名成 DEST**（壞包／磁碟滿／被中斷時 DEST 根本不存在，不留一棵解到一半的樹讓人誤當還原結果）；**包內容是不可信輸入**（票 12）——`verify_manifest()` 以 `O_NOFOLLOW|O_NONBLOCK` 開檔＋`fstat` 判 regular＋1 MiB 上限＋解 JSON＋**驗形狀**〔頂層是物件、`accounts` 是 mapping、`extra` 是 {str: str}——與 `install.py::read_manifest` 同一組判準——**`created` 刻意不列入**：它只影響顯示、不參與任何路徑或安全決策，為一個顯示欄位拒絕整個包是過度（顯示端改用 `.get` 容忍，原本硬取會在**已發布之後**KeyError traceback，既有疏漏）；少了這道，`accounts` 是 list 的包會一路走到差異報告才 AttributeError traceback，而那時 DEST **已經發布**：使用者看到一個「看起來完成」的展開目錄配一段 traceback、沒有差異報告〕，取代原本跟隨 symlink 的 `[ -f ]`＋`json.load(open(...))`〔一份把 manifest.json 做成 symlink 的包會讓腳本解析包外的檔案並印在畫面上，實測成立；**必須在發布前擋**——`rename` 整棵樹時 symlink 會跟著過去〕，錯誤訊息刻意不含檔案內容，且 `--diff-only` 那條路徑同樣要驗（它跳過解壓、讀的展開目錄不是本次驗過的）；**與 `backup/install.py::read_manifest` 是同一組判準的兩份實作**（腳本不能 import sidecar），改一邊要改另一邊。**`verify_manifest` 只驗語法不驗語意**（inode／大小／JSON 合法性），所以差異報告的**現役側路徑不得取自 manifest**——`accounts`／`extra` 的 value 是備份包提供的不可信輸入，直接拿去`expanduser`＋遞迴 `snapshot` 等於讓一份惡意包指定掃描本機任意目錄並把檔名印在報告裡〔指向 `/` 就是整個檔案系統，**已實測可利用**〕；**但也不能拿 manifest 的 path 去跟本機路徑比字串**——manifest 記的是**舊機**絕對路徑，移機換了使用者名或落點後必然不相等，於是每個帳號都被略過、報告變成一份**假的空報告**，而移機正是這批票的主題〔R1 的修法方向錯誤，R2 抓到〕。`local_live_paths()` 因此改成 **manifest 的 key → 本機 config 同 key 的路徑**（key 穩定、path 不穩定）：帳號查 config 的 accounts、帳號外資產查 config 的 extra 再以 `backup-extra-paths.txt` 的 basename 補（與 `backup-claude.sh` 產 manifest 同一條規則，config 已有的不覆蓋）；本機沒登記該 key 就略過比對，**不退回 manifest 的路徑**；config 讀不出來時只認預設帳號 key → `~/.claude` 一個對應。manifest 的 path 只拿來顯示「這包來自哪裡」。**key 本身也是不可信輸入**——它被拼進 `os.path.join(dest, "accounts", k)`，`../../x` 這種 key 能讓比對的備份側走出展開目錄；查表查不到固然也會略過，但那是副作用不是防線（調換判斷順序或加個 fallback 就破功），故 `safe_keys()` 逐個驗名，壞 key 當場說明並跳過而**不整份拒絕**（一個壞 key 不該讓其餘正常帳號的差異也看不到）。**帳號與 extra 用不同判準**：帳號 key 是使用者在 Fledge 內自己取的，走 `^[A-Za-z0-9_-]+$`（與 `backup/install.py::_SAFE_KEY_RE` 同一條信任邊界）；**extra 的 name 是 basename 衍生的**（`backup-claude.sh` 用 `basename`，真實的 `~/.agents` 產出的就是 `.agents` 帶前導點），走「安全的單一路徑元件」判準〔非空、非 `.`／`..`、不含 `/` 與 NUL〕——套帳號那組字元會把合法的 extra 整個擋掉。**sidecar 的 `validate_landing_spots` 目前正是套錯那組、拒絕自己產生的 `.agents`，見票 13**。`check_expansion()` 三道：**項目數**上限 20 萬〔實測本機真實 `~/.claude` 是 1.5 萬項，留 13 倍餘裕；**大小配額擋不住「多」**——symlink／目錄／零大小檔的 st_size 全是 0，幾十萬個這種成員可以讓總量停在 0 而完全繞過比例判準〕、**壓縮比**擋 tar bomb〔8 MiB 以下不看比例（gzip 固定開銷會讓小包失真）、超過 200× 拒絕，比較不用整數截斷否則 200.9× 會過〕、加總只算一般檔且以`(st_dev, st_ino)` **去重 hardlink**〔逐名加總會把一份內容算 N 次，誤擋方向雖安全仍是誤擋〕——**宣稱等級：不是「防止磁碟耗盡」也不是「防止 inode 耗盡」**，解壓當下兩者都已被佔掉〔磁碟真滿是 tar 自己失敗、由既有 cleanup 處理〕，這一層擋的是「不讓一個離譜的展開目錄留在磁碟上」（大得離譜或項目多得離譜都算）；擋得住「宣告小、實際大」（量的是實際落地的東西），擋不住「解壓過程中就把磁碟塞爆」（那需要 streaming 計量）。判準用比例而非絕對大小，後者會誤擋誠實的大包。路徑穿越（絕對路徑成員／`..`／透過包內 symlink 往外寫）**靠 libarchive 預設就擋掉**〔三者實測皆被拒〕，刻意不自己寫受控 extractor，改用測試釘住那三個預設行為；備份包來源必填（給路徑或設 `FLEDGE_BACKUP_DIR`，無寫死預設）。差異比清單與大小不比內容（500MB 逐位元太慢）；`snapshot()` **先記 symlink 再從 `dirnames` 移除**，順序不能反——`os.walk` 把有效的 symlink 目錄歸 `dirnames`、斷鏈的歸 `filenames`，先過濾會讓同一條連結在現役與備份兩邊落入不同分類、報成假的「現役已不見」 | — |
| `scripts/make-probe-account.sh` | 共通設置卡（票 26／29）驗收用：造一個拋棄式帳號目錄湊齊各種項目狀態（`--fresh` 全待建立／預設混合：非空實體目錄→保留不動、缺項→將建立連結、連錯位置→將重新指向、已正確連結→已就緒、`CLAUDE.md` 內容不同→保留不動），`--clean` 收掉。**安全不變式：只刪自己建的目錄**——判準是目錄內的 `.fledge-probe` 哨兵檔，目錄已存在卻沒有哨兵一律拒絕；第二道以 realpath 比對 `config.json` 的已登記帳號目錄（讀不到設定檔不阻擋，哨兵才是主防線）。此腳本的第一版把「已登記帳號」檢查寫成設定檔不存在就靜默跳過（fail-open），驗防呆時把 `~/.claude-tc` 的內容 `rm -rf` 掉了 |
| `scripts/build-app-devtools.sh` | 偵錯打包：sidecar + `tauri build --features devtools`（帶 Web Inspector，啟動自動開）；給「只有打包版重現、dev 正常」的 bug 蒐證用。`npm run build:app:devtools`。正式 build 不帶 feature、不外洩 devtools |
| `sidecar/pyproject.toml` | Python 依賴與測試設定 |
| `vitest.config.ts` | 前端 vitest 設定（store lifecycle 測試）；掛 `setupFiles` |
| `vitest.setup.ts` | 測試端環境 shim：Node 25 在 globalThis 放無方法的 `localStorage` 空殼並蓋掉 jsdom Storage，讓任何讀寫 localStorage 的模組一 import 就 TypeError；偵測到空殼才換上 in-memory Storage（正式執行環境不經過此檔） |

### admin/ — 維運腳本
| 檔案 | 用途 |
|------|------|
| `sync_pricing.py` | 從 LiteLLM 的 `model_prices_and_context_window.json` 重生 `usage/pricing_table.py`（手動執行，不在 runtime 連外——理由見 ADR-0005）。只收第一方裸 key（濾掉 `vertex_ai/`、Bedrock `-v1:0` 等區域價）；表 key 用 pricing.py 自己的 normalize 產生；cache 價一律「上游真價優先，缺值或偏離標準倍率逾 2 倍（＝資料損壞）才推導並報告」，兩源共用同一條規則（Claude 三層 1.25/2/0.1、Codex cached 0.1）。**寫檔前的阻斷條件**（整檔覆蓋是破壞性的）：上游非空 mapping、同一正規化名不得對到不同價、每個價須為有限正數（缺 `output` 會變 0＝output 免費）、移除既有模型需 `--allow-removals`。寫檔後掃本機用量報「仍查無定價」的模型。`--dry-run`／`--from <json>`／`--no-check-local`／`--allow-removals` |

### docs/agents/ — Agent skills 設定（mattpocock engineering skills 讀取）
| 檔案 | 用途 |
|------|------|
| `issue-tracker.md` | Issue 追蹤方式：local markdown（`.scratch/<feature-slug>/`，已 gitignore），一張 ticket 一個檔、`Status:` 行記 triage 狀態 |
| `triage-labels.md` | 五個 triage label 對應表（沿用預設字串） |
| `domain.md` | Domain 文件讀取規則：single-context（根目錄 `CONTEXT.md` + `docs/adr/`，由 skills 惰性建立） |

---

## 影響鏈

> 修改左側檔案時，Claude 必須提醒使用者檢查右側關聯檔案。

| 修改了... | 需要檢查... |
|-----------|------------|
| `sidecar/.../routes/*.py`（endpoint / 參數變更） | `src/lib/sidecar.ts`、對應前端元件、`sidecar/tests/test_*.py` |
| `sidecar/.../pty_bridge.py`（Session 介面變更） | `routes/sessions.py`、`src/components/Terminal.tsx`、`test_pty_bridge.py` |
| `src/components/Terminal.tsx`（onmessage / teardown 變更） | §7 終端機不變式、`src/lib/activityTracker.ts`、`src/components/EnvCard.tsx`／`LoginCard.tsx`（`Workspace` 之外的掛載點：卡片內安裝／登入終端機，走合成 tabId、store 內無對應 tab） |
| `src/lib/sidecar.ts` 的 `createSession`（簽名／body 欄位變更） | `src/store/useAppStore.ts`（openTab）、`src/components/EnvCard.tsx`（kind=install）、`src/components/LoginCard.tsx`（kind=login）、`sidecar.test.ts`、`useAppStore.test.ts`、`sidecar/.../routes/sessions.py` 的 `CreateSessionRequest`（`extra="forbid"`，欄位不對即 422） |
| `src/lib/clipboard.ts`（寫入行為／回傳語意變更） | `src/components/Terminal.tsx`（選取複製）、`src/components/EnvCard.tsx`（複製指令回饋） |
| `src/store/useAppStore.ts` 的 `bootstrap`（序列步驟／回傳型別／`startup` 狀態機變更） | `src/App.tsx` 的 `runStartup`（**回傳值的唯一消費者**——新增副作用一定要加在這裡，否則 Splash 重試那條路徑會漏）、`src/components/Splash.tsx`（讀 `startup`／`startupError` 決定等待態與錯誤態）、`src/store/bootstrap.test.ts`、`src/App.startup.test.tsx` |
| `src-tauri/src/sidecar.rs` 的 `spawn_sidecar`／`spawn_error`（簽名或錯誤語意變更） | `src-tauri/src/lib.rs`（setup 的錯誤處理與 command 註冊）、`src/lib/sidecar.ts` 的 `waitForSidecarPort`（查 `sidecar_spawn_error` 快速失敗）、`src/components/Splash.tsx`（詳細資訊顯示該原文） |
| `src/lib/sidecar.ts` 的 `AppConfigData` 欄位（新增 UI 會直接索引的欄位） | `src/lib/appConfigGuard.ts`（新欄位若被元件直接索引就必須納入驗證，否則畸形資料會在 Splash 淡出後才炸）、`src/lib/appConfigGuard.test.ts`、`sidecar/.../app_config.py` |
| `sidecar/.../app_config.py`（config schema 變更） | `~/.fledge/config.json` 結構、onboarding/設定頁、`test_app_config.py` |
| `sidecar/.../app_config.py` 的 `DEFAULT_CONFIG["accounts"]`（預設帳號 key／數量變更） | `src/lib/sidecar.ts` 的 `DEFAULT_ACCOUNT_KEY`（config 未載入時的暫時值）、`src/lib/accountColor.ts`（預設 key 的固定配色）、`Settings.tsx`／`Onboarding.tsx` 的 select 初值與 fallback、`test_app_config.py`（釘住預設值的測試）、`test_config_routes.py` 的 onboard fixture（送的 `default_account` 必須是預設帳號）、`test_setup_routes.py` 的 readiness 測試（未 onboard 時湊不出 source→target） |
| `sidecar/.../paths.py`（正規化/驗證行為變更） | `routes/config.py`、`routes/projects.py`、`app_config.py`（load 遷移）、`test_paths.py` 及上述各 test |
| `sidecar/.../__main__.py`（port 協定變更） | `src-tauri/src/sidecar.rs`（抓 port 的 prefix 比對） |
| `src-tauri/tauri.conf.json`（`bundle.resources` 名/路徑變更） | `sidecar/build_binary.sh`（onedir 資料夾命名）、`src/sidecar.rs`（`resource_dir().join("fledge-sidecar")` 路徑） |
| `sidecar/pyproject.toml`（新增套件） | `build_binary.sh`（PyInstaller hidden imports） |
| `src/components/Sidebar.tsx`（分組邏輯變更） | `src/lib/sidebarGroups.ts`、`src/lib/sidebarGroups.test.ts` |
| `store/useAppStore.ts`（Tab 模型變更） | `src/App.tsx`（共用 isLiveClaudeTab predicate）、`src/components/TabBar.tsx`、`src/components/Terminal.tsx` |
| `sidecar/.../usage/pricing.py`（正規化/公式/例外清單變更） | **儀表板所有成本數字**；`test_usage_pricing.py`；改 `PINNED`／`EXCLUDED` 後要重跑 `admin/sync_pricing.py` 讓表跟上。**改 `normalize_*_model` 必須升 `usage/cache.py` 的 `SCHEMA_VERSION`**——L2 存的是正規化後的 model 名，`pricing_version` 只觸發重算不重 parse，舊名會用新表繼續算下去 |
| `sidecar/.../usage/pricing_table.py`（定價數字變更） | **儀表板所有成本數字**；不得手改——跑 `python admin/sync_pricing.py` 重生（`TABLE_VERSION` 自動隨內容變，L2 只重算 cost 不重 parse） |
| `sidecar/.../usage/parser.py`（UsageEntry 欄位變更） | `usage/cache.py`（L2 序列化 roundtrip——欄位變更需升 `SCHEMA_VERSION`）、`usage/aggregator.py` |
| `sidecar/.../routes/usage.py`（payload shape 變更） | `src/lib/sidecar.ts`（`UsageDashboard`/`CodexUsage` 型別）、`src/components/Dashboard.tsx`、`test_usage_routes.py` |
| `sidecar/.../api/codex_usage.py`（端點/回傳合約變更） | `routes/usage.py`（`/usage/codex`）、`src/lib/sidecar.ts`（`CodexUsage`）、`Dashboard.tsx`（`CodexPanel`）、`test_codex_usage.py` |
| `sidecar/.../usage/account_activity.py`（事件格式/歸屬規則變更） | `routes/sessions.py`（寫 open/close）、`pty_bridge.py`（live_ids/on_close 契約）、`test_account_activity.py` |
| `app_config.subscriptions`（結構變更） | `routes/config.py` PUT 驗證、`src/lib/subscriptionsForm.ts`（含 `dropBlankRows`／`sameSubscriptions`）、`Settings.tsx`、`SystemSettingsCard.tsx`（精靈版表單）、KPI Net ROI |
| `src/locales/*/dashboard.json`（key 增刪） | 另一語言 catalog 同步（`i18nCatalog.test.ts` parity 會擋）、`Dashboard.tsx`/`Settings.tsx`/`Sidebar.tsx`/`TabBar.tsx` 的 t() 引用 |
| `sidecar/.../memory/aggregator.py`（payload shape 變更） | `src/lib/sidecar.ts`（`MemoryOverview`/`MemoryItem` 型別）、`Memory.tsx`/`RelatedFloat.tsx`、`test_memory_aggregator.py` |
| `sidecar/.../memory/scanner.py`（`is_kms_file_allowed`/掃描範圍變更） | `routes/memory.py`（`/item` containment 共用同一 allowlist）、`test_memory_scanner.py`、`test_memory_routes.py` |
| `sidecar/.../routes/memory.py`（endpoint/payload 變更） | `src/lib/sidecar.ts` memory fetchers、`Memory.tsx`+左右欄元件（`MemoryIndex`/`MemoryGroup`/`MemoryRow`/`MemoryDetail`/`SuggestionChip`）/`RelatedFloat.tsx`、`test_memory_routes.py` |
| `src/lib/memoryView.ts`（VM/facet/selection 邏輯變更） | `Memory.tsx`、`MemoryIndex`/`MemoryGroup`、`memoryView.test.ts` |
| `src/locales/*/memory.json`（key 增刪） | 另一語言 catalog 同步（`memory-parity.test.ts` 會擋）、`Memory.tsx`+記憶元件群/`RelatedFloat.tsx`/`Sidebar.tsx`/`TabBar.tsx`/`Settings.tsx` 的 t() 引用 |
| `sidecar/.../dir_tree.py`（列目錄邏輯變更） | `routes/projects.py`、`test_dir_tree.py` |
| `sidecar/.../paths.py`（containment helper 變更） | `routes/projects.py`、`test_paths.py` |
| `sidecar/.../routes/projects.py`（tree endpoint 變更） | `src/lib/sidecar.ts`、`Sidebar.tsx`/`FileTree*.tsx`、`test_projects.py` |
| `sidecar/.../routes/setup.py`／`setup/*.py`（偵測 payload/資料表／共通設置／範本合約變更） | `src/lib/sidecar.ts`（`ToolStatus` 型別與 `fetchSetupStatus`；共通設置的 state/action/outcome union 與 `COMMON_CONFIG_ENTRIES`）、`src/components/EnvCard.tsx`（依 `tier`／`install_command`／`manual_command` 決定列什麼）、`src/components/CommonConfigCard.tsx`（`STATE_TEXT`／`ACTION_CHIP`／`OUTCOME_TONE` 三張窮舉表；後端加 state／action 會編譯失敗，加判別碼則靜默退通用訊息——需一併補 catalog 的 `errors.<code>`）、`src/components/TemplateCard.tsx`（`STATE_CHIP`／`OUTCOME_TONE` 兩張窮舉表＋`ERROR_TEXT` 判別碼表）、`src/components/RestoreCard.tsx`（斷鏈偵測數 `broken_link`、修復送 `RESTORE_REPAIR_ENTRIES`）、`CommonConfigCard.test.tsx`、`EnvCard.test.tsx`、`TemplateCard.test.tsx`、`RestoreCard.test.tsx`、`test_setup_routes.py`、`test_env_detect.py`、`test_install_specs.py`、`test_common_config.py`、`test_templates.py`、`test_template_manifest.py` |
| `sidecar/.../setup/templates.py`（`TEMPLATE_SPECS` 增刪／`source_class` 變更） | `setup/template_manifest.py`（分類唯一真實來源）、`sidecar/resources/templates-public/`（頂層目錄名須在 allowlist）、`.github/workflows/release.yml`（seed 分類與 drift 檢查）、`test_templates.py`、`test_template_manifest.py` |
| `sidecar/build_binary.sh`（範本 staging 變更） | `scripts/verify_templates_artifact.py`、`.github/workflows/release.yml`、`setup/template_manifest.py`、`test_release_workflow.py` |
| `sidecar/resources/templates-public/**`（seed 內容增刪） | 同目錄的 `manifest.json` 必須一起重產（`build_manifest_entries`），否則 `test_templates.py` 的 drift 測試與 release workflow 的 ③ 都會失敗 |
| `src/lib/tabOrder.ts`（排序/插入邏輯變更） | `store/useAppStore.ts`、`tabOrder.test.ts` |
| `src/lib/terminalRegistry.ts`（handle contract 變更） | `Terminal.tsx`、`App.tsx`（onDragEnd 貼路徑） |
| `src/lib/onboardingSteps.ts`（頁面序列增刪／降級規則變更） | `Onboarding.tsx`（每個 `step` 一個渲染分支；移機的落點頁是獨立的 `targets` 身分，不與 `roots` 共用、渲染不必判 `mode`）、`onboardingSteps.test.ts`、`Onboarding.test.tsx`、`.scratch/onboarding-wizard/spec-b4.md` 定案 9（全新設定序列）、`docs/superpowers/specs/2026-07-31-restore-into-empty-target-design.md` §5.1（移機序列） |
| `src/locales/*/sidebar.json`（key 增刪） | 另一語言 catalog 同步（`sidebar-parity.test.ts` 會擋）、`Sidebar.tsx`/`TabBar.tsx`/`FileTree*.tsx`/`Terminal.tsx` 的 t() 引用 |
| `src/locales/*/restore.json`（key 增刪） | 另一語言 catalog 同步（`restore-parity.test.ts` 會擋）、`RestoreCard.tsx` 的 `CODE_KEY` 映射表與 `t()` 引用；後端新增判別碼時**兩語都要加**（前端沿用 `i18n.exists` 動態查表，沒有文案就退通用訊息） |
| `src/locales/*/onboarding.json`（key 增刪） | 另一語言 catalog 同步（`onboarding-parity.test.ts` 會擋）、`Onboarding.tsx` 與 B-4 各設置卡（含 `DevEnvSection`）的 t() 引用 |
| `src/components/CommonConfigCard.tsx`（props／行為變更） | **兩處掛載**：`Onboarding.tsx`（精靈版，無 `allowOverwrite`）與 `DevEnvSection.tsx`（設定頁版，逐項授權）、`CommonConfigCard.test.tsx` 的兩個 describe |
| `src/components/TemplateCard.tsx`（props／行為變更） | **兩處掛載**：`SystemSettingsCard.tsx`（精靈系統設置頁第三區）與 `DevEnvSection.tsx`（設定頁常用卡）、`TemplateCard.test.tsx` |
| `src/lib/useCardSession.ts`（session 生命週期契約變更） | `EnvCard.tsx`（安裝）／`LoginCard.tsx`（登入）兩個消費者、`CardTerminal.tsx`（合成 tabId 的掛載點）、`store/useAppStore.ts` 的 `setTabStatus`／`setTabActivity` 找不到 tabId 即原樣返回的約定 |

---

## 環境變數清單

| 變數名 | 用途 | 預設值 | 必填 |
|--------|------|--------|------|
| `CLAUDE_CONFIG_DIR` | 傳給 `claude` subprocess 切換帳號（工作/私人） | `~/.claude` | 由 sidecar 動態設 |
| `FLEDGE_CONFIG_PATH` | 覆蓋設定檔路徑（測試用） | `~/.fledge/config.json` | ❌ |
| `FLEDGE_TEST_COMMAND` | 測試模式替代 `claude` 的命令（如 `cat`） | 無（正常跑 claude） | ❌（僅測試） |
| `FLEDGE_TOKEN` | sidecar 認證 token（HTTP `X-Fledge-Token` / WS `?token=`） | 無 | 由 Tauri 殼 per-launch 生成注入（prod 必有；不灌進 claude 子進程） |
| `FLEDGE_TEST_UNAUTH` | 設 `1` 關閉認證（測試/手動執行 opt-out；conftest autouse 預設設此） | 無 | ❌（僅測試/手動） |
| `FLEDGE_CODEX_HOME` | 覆蓋 codex 資料根（觀測掃描；測試用） | `~/.codex` | ❌ |
| `FLEDGE_USAGE_CACHE` | 覆蓋觀測 L2 快取路徑（測試用） | `~/.fledge/cache/usage-v1.json` | ❌ |
| `FLEDGE_MEMORY_LINKS` | 覆蓋記憶層連結存儲路徑（測試用） | `~/.fledge/memory-links.json` | ❌ |
| `FLEDGE_TEMPLATES_DIR` | 覆蓋範本根目錄（測試用） | 凍結時 `_MEIPASS/templates`，否則 repo `sidecar/resources/templates-public` | ❌ |
| `FLEDGE_PRIVATE_TEMPLATES_DIR` | build 時私有範本 staging 來源（僅搭配 `--with-private-templates`；沒帶 flag 時完全不生效，release 禁止設定） | 無 | ❌ |

---

## 備註

- 本文件由開發者維護，Claude Code 在結構變更時應提醒更新
- 影響鏈僅為提醒，不代表一定要修改右側檔案
- 韌性、path 正規化、sidecar 認證 token + CORS 收緊、桌面打包（onedir sidecar + curl/dmg 安裝，macOS arm64）均已完成。
- 記憶層（跨專案連結層）：唯讀聚合 native memory + Obsidian KMS，唯一寫入 `memory-links.json`；後端 `memory/`（parser→scanner→links→aggregator→routes/memory），前端 memory 分頁 + 終端機右下懸浮。對來源永遠唯讀，`/memory/item` 以 realpath containment 守邊界。
